# 新活动需求单模板

> 本文件可以复制到实际活动源码仓库的 `docs/`。不要填写凭据。

## 1. 基本信息

| 字段 | 内容 |
| --- | --- |
| 活动显示名 |  |
| `activityId` |  |
| 负责人 |  |
| 计划日期 |  |
| 最少/最多人数 |  |
| 单局时长 |  |
| 最低平台等级 | `Participant` / 其他 |
| 新活动或现有活动升级 |  |

## 2. 玩法定义

- 一句话目标：
- 开始条件：
- 回合阶段：
- 胜负条件：
- 玩家死亡后的状态：
- 观察者允许的动作：
- 断线重连规则：
- 服务端重启后的恢复规则：
- 管理员命令：
- 不得改变的玩法参数：

## 3. 技术栈

| 字段 | 内容 |
| --- | --- |
| Minecraft |  |
| 加载器 | Vanilla / Paper / Purpur / Fabric / Forge / NeoForge |
| 加载器版本 |  |
| Java 主版本 |  |
| 客户端必须安装 |  |
| 客户端可选 |  |
| 服务端专用 |  |
| 两端共同 JAR |  |
| 第三方依赖与许可证 |  |
| 协议版本 |  |

原生 Fabric/Forge/NeoForge 不填写 Bukkit 插件。需要插件和模组同时存在时先停止设计，
说明为什么不能实现为对应加载器模组，并单独评估核心与兼容风险。

## 4. 基础组件计划

- 组件计划文件：`docs/<activity-id>-component-plan.json`
- 参考模板：`docs/examples/server-baseline/component-plan.example.json`
- 计划状态：draft / reviewed / deployed / verified

| 层级/能力 | 动作 | 制品版本与 SHA-256 | 所有者 | 验证与回滚 |
| --- | --- | --- | --- | --- |
| Velocity Authorizer 单例 | 核验，不复制 |  | 平台 |  |
| Lobby Guard / Tier Agent | 排除 |  | 内部大厅 |  |
| ServerControlAgent | 注册控制目标 |  | VPS 主机 |  |
| StatusCollector | 注册状态/指标目标 |  | VPS 主机 |  |
| 世界备份与告警 | 注册目标 |  | VPS 主机 |  |
| Velocity forwarding | 安装/原生配置/阻塞 |  | 后端身份 |  |
| TPS/MSPT/GC 指标 | 安装兼容实现/阻塞 |  | 平台指标 |  |
| 活动共同/服务端组件 | 白名单部署 |  | 活动 |  |

- forwarding 实现、精确适用版本和复核证据：
- 指标实现、精确适用版本和输出路径：
- 明确排除的大厅/旧服/通用插件：
- 未解决的生产阻塞项：
- 是否确认没有复制旧服完整 `plugins/mods/config`：是 / 否

组件计划不得包含密钥、令牌或凭据。没有兼容 forwarding 或指标实现时写“阻塞”，不把
近似版本 JAR 强装进服务端。

## 5. 平台标识

| 字段 | 内容 |
| --- | --- |
| `serverId` | `activity-...` |
| `controlTargetId` | 默认与 `serverId` 相同 |
| `profileId` | `activity-<id>-<loader>-<mc-version>` |
| `velocityTarget` | 固定 `activity` |
| `backendHost` | 固定 owl5 `127.0.0.1` |
| `backendPort` | 固定 `25568` |
| `conflictGroup` | 固定 `owl5-activity-slot` |
| 服务端目录 |  |
| 计划任务 |  |
| 内存参数文件 |  |
| Xms / Xmx / 单服上限 |  |

## 6. 地图与数据

- 地图来源和授权：
- 世界版本：
- 是否覆盖旧世界：
- 玩家数据是否保留：
- 必须保留的配置：
- 应干净替换的 `mods/plugins/config`：
- 部署前备份位置：
- 回滚世界/服务端版本：
- 恢复验证方法：

## 7. 性能预算

- 目标人数：
- 目标持续 TPS：不少于 `19.5`：
- MSPT 95 分位：低于 `50 ms`：
- 同屏实体上限：
- 同时加载区块预算：
- 每 tick 生成/放置预算：
- 网络包频率与最大长度：
- 后台任务队列上限、超时与取消：
- 性能分析工具与采样窗口：

## 8. 冲突所有权

每项只能有一个明确所有者：

| 功能 | 所有者 | 已审计冲突组件 |
| --- | --- | --- |
| nametag |  |  |
| TAB |  |  |
| 计分板队伍 |  |  |
| 发光效果 |  |  |
| 死亡/墓碑 |  |  |
| 传送/集合 |  |  |
| 公告 |  |  |
| 角色分配 |  |  |
| 物资生成 |  |  |
| 活动命令 |  |  |

## 9. 测试和发布

- [ ] 纯逻辑和边界人数测试
- [ ] 协议编解码、长度、频率和错误版本测试
- [ ] 无图形专用服务端启动
- [ ] 双客户端同步与断线重连
- [ ] 死亡观察者权限拒绝
- [ ] `1/2/7/20` 人矩阵
- [ ] 客户端干净档案、签名、全对象校验和修复
- [ ] 服务端备份和回滚演练
- [ ] 基础组件计划无阻塞项且部署层正确
- [ ] forwarding、指标、服控、状态、备份和告警已验证
- [ ] Test 通道
- [ ] Gray 通道
- [ ] Production 通道
- [ ] 管理员 fresh grant 真实进服
- [ ] `2/3/5/20` 人灰度
- [ ] Git 提交、推送、发布记录和标签

## 10. 生产授权

- 允许部署到 VPS：是 / 否
- 部署后允许启动：是 / 否
- 允许把目录设为 Online：是 / 否
- 允许进入 2/3/5/20 人哪一级：
- 授权人和日期：

没有明确“是”的动作保持未执行，不从上下文猜授权。
