# 赫朝启动器 0.11.3 发布记录

> 构建日期：`2026-07-26`
>
> 启动器源码提交：`876d905190e246cf906167183546e8b7c0e41db9`
>
> 配套 API：`0.11.1-20260725T165050Z`，本次没有服务端变更
>
> 状态：本机内部候选已验证；安装包尚未上传 OSS 或面向玩家发布

## 修复范围

| 用户反馈 | 实现 |
| --- | --- |
| 下载进度条没有移动反馈 | 新增 `AnimatedProgressBar`，将真实百分比映射为 180 ms EaseOut 动画；显示值不会反写下载任务，关闭 Windows 控件动画时立即更新 |
| 服务器页右侧滚动条多余 | 服务器详情页隐藏滚动条但保留滚轮滚动；其他长页面使用 9 像素透明轨道和圆角滑块 |
| 游戏数据目录底边像被裁切 | 目录行从固定高度改为自动测量，面板最小高度由 `292` 增至 `332`，上下区域增加底部留白 |
| 点击内容区出现虚线框 | 所有 `ScrollViewer` 关闭默认 `FocusVisualStyle`；按钮、文本框自己的焦点与悬停状态不受影响 |
| 登录/注册页签右边框被裁切 | 页签模板边框向内缩进 1 像素并启用设备像素对齐，页签宽高和间距同步增加 |

## 验收证据

- 1500 x 860 实机预览确认服务器页无右侧滚动条，游戏数据目录四边完整，点击内容区后无虚线焦点框。
- 登录与注册两个选中状态均完成实机切换，左右、上下边框完整。
- 真实 WPF 动画测试将进度从 `0%` 调到 `80%`：初始显示接近 `0%`，70 ms 时处于中间值，260 ms 后稳定在 `80%`。
- XAML 契约测试确认两条下载进度均使用动画控件和单向绑定，目录行使用 `Auto`，服务器滚动条为 `Hidden`，页签边框保持内缩。
- 完整解决方案 `191/191` 项自动测试通过；正式安装器构建过程再次运行同一套测试并通过。
- 已从 `0.11.2` 静默覆盖升级到正式候选，安装后程序正常响应；设置文件、结构版本 `2` 的客户端根目录及基础档案 `base-1.21.11` `1.0.5` 均保留。本机验收时没有现存 DPAPI 会话文件，因此本次不声明登录状态保留。

## 制品

| 项目 | 值 |
| --- | --- |
| 启动器 EXE | `D:\Hechao Launcher\Hechao.Launcher.exe` |
| EXE ProductVersion | `0.11.3+876d905190e246cf906167183546e8b7c0e41db9` |
| EXE 大小 | `68,636,908` 字节 |
| EXE SHA-256 | `CEB4AD2B69260941028CED2E4BBC7F8A11F39450FF79F8B9D4A89C3B3194733E` |
| 安装包 | `artifacts/installer/Hechao-Launcher-Setup-0.11.3-win-x64.exe` |
| 安装包大小 | `61,803,987` 字节 |
| 安装包 SHA-256 | `FDEB88A559A94D37FD70F059104E8DAD68AD347FA6E51ABF8943F166606824A9` |
| Windows 签名 | EXE 与安装包均为 `NotSigned` |

## 边界

本次只修改 Windows 启动器界面和对应测试。没有变更 API、数据库、OSS 对象、客户端档案、Velocity 或任何 Minecraft 服务器；没有调用发布器、上传安装包或生成下载链接。
