# 天域远征工业季客户端档案 1.0.19 Test 发布

- 发布日期：2026-08-18
- 档案 ID：`skyrealm-industrial-neoforge-1.21.1`
- 不可变版本：`1.0.19`
- 清单 SHA-256：
  `ABC8B15B512686D35981A378B9D0A92B4AD5A64EE0C290F8865158A8FC470528`
- Test：`r12 / 1.0.19 / 100%`
- Gray：未分配，`r1`
- Production：未分配，`r1`

## 发布结果

- `1.0.18` 与 `1.0.19` 的 `4,456` 个共同文件路径、大小和 SHA-256 全部不变；
- 删除 Screen `0.1.9`，新增 Screen `0.1.10`，同路径内容变化为 `0`；
- 逻辑文件 `4,457`，逻辑大小 `1,204,122,917` 字节；
- 去重对象 `4,252`，对象大小 `1,204,114,407` 字节；
- OSS 对 `4,251` 个既有对象完成复核，仅上传 `1` 个新对象、`841,439` 字节；
- 匿名清单、对象和后台 API 请求均返回 `401`。

生产 API 已按原始字节保存签名清单，远端与本地大小均为 `2,025,516` 字节，回读
SHA-256 与本地一致。远端清单权限为 `0640`、所有者为 `hechao-api:hechao-api`。
审计 `9075` 记录签名版本导入，审计 `9076` 记录 Test 通道更新；API `0.34.0` 健康、
就绪、数据库与服务状态均正常，`NRestarts=0`，发布窗口内 warning 以上日志为 `0`。
生产商品目录实时回读保持 `85/85` 项启用。没有操作任何 Minecraft、Velocity、API、
代理或服控进程。

## 回滚与剩余验收

Test 回滚目标为 `1.0.18` 清单
`9BE857DAEAD9743D79C96F04917E4040B5796153A9BA5C91826E3B51809562EB`。回滚只移动
Test 指针，不删除 `1.0.19` 清单和对象。

玩家完全退出旧游戏后，由启动器执行增量更新并重新启动，再验收导航、余额、出售结果和
回收目录的背景裁切、面板清晰度、按钮点击、Tooltip 与分页。真人验收完成前不得推进
Gray 或 Production。

