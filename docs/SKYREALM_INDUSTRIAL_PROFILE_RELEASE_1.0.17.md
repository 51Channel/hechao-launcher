# 天域远征工业季客户端档案 1.0.17 Test 发布

- 发布日期：2026-08-18
- 档案 ID：`skyrealm-industrial-neoforge-1.21.1`
- 不可变版本：`1.0.17`
- 清单 SHA-256：
  `96433353B56F115770C819A487ED4B8BD1F85C1082A4124EA42B31E646CE44A2`
- Test：`r10 / 1.0.17 / 100%`
- Gray：未分配，`r1`
- Production：未分配，`r1`

## 发布结果

- `1.0.16` 与 `1.0.17` 的 `4,456` 个共同文件路径、大小、SHA-256 和对象 URL 全部一致；
- 删除 Screen `0.1.7`，新增 Screen `0.1.8`，同路径内容变化为 `0`；
- 逻辑文件 `4,457`，逻辑大小 `1,203,333,576` 字节；
- 去重对象 `4,252`，对象大小 `1,203,325,066` 字节；
- OSS 对 `4,251` 个既有对象完成复核，仅上传 `1` 个新对象、`52,098` 字节；
- 匿名清单、对象和后台 API 请求均返回 `401`。

生产 API 已按原始字节保存签名清单，远端与本地大小均为 `2,025,517` 字节，回读
SHA-256 与本地一致。远端清单权限为 `0640`、所有者为 `hechao-api:hechao-api`。
审计 `8991` 记录签名版本导入，审计 `8992` 记录 Test 通道更新；API `0.33.1` 健康、
就绪、数据库与服务状态均正常，`NRestarts=0`，发布窗口内 warning 以上日志为 `0`。
没有操作任何 Minecraft、Velocity、API、代理或服控进程。

## 回滚与剩余验收

Test 回滚目标为 `1.0.16` 清单
`91243AC25E78068E2514EC635BC2B42C2B5E3EC09876C1B2BCDF6D89E13E1A64`。回滚只移动
Test 指针，不删除 `1.0.17` 清单和对象。

玩家完全退出旧游戏后，由启动器执行增量更新并重新启动，再验收余额结果页、出售报价页
和回收目录的文字、面板与 Tooltip 清晰度。生产商品目录仍为空；完成真人验证前不得推进
Gray 或 Production。
