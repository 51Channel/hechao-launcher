# API 0.20.0 发布记录

> 状态：已生产部署并完成公网回归
>
> 发布 ID：`0.20.0-20260727T011953Z`（当前为 `0.20.1` 的直接回滚目标）
>
> 源码提交：`b5c1a78`

## 1. 变更

- 新增迁移 17，保存分钟请求指标、当前运行告警和告警变化历史。
- 新增固定规则的 API 5xx、延迟、登录失败、对象授权失败和客户端下载失败评估。
- 新增游戏服心跳与 Paper/Purpur Tick 指标告警。
- 新增内部合成告警写入/读取端点，使用独立令牌哈希和速率限制。
- 管理后台增加“运行告警”页面。
- 新增独立平台监控器 `0.1.0`，检查公网入口、TLS 证书和异地备份状态，并通过邮件发送
  告警变化与恢复。

完整边界见 [`OPERATIONAL_ALERTS.md`](OPERATIONAL_ALERTS.md)。

## 2. 制品

| 制品 | 大小 | SHA-256 |
| --- | ---: | --- |
| `hechao-api-0.20.0-20260727T011953Z.tar.gz` | `45,575,077` | `874C05ECE9AA6DE628C1B7E99191D8E7FB50745E7224059D1F5D8C90E95A8665` |
| `Hechao.Api` | `104,441,907` | `67C3E084D9E53509B283A4B39498219C33BF1676BB4F1805A916E83CFFABBDEB` |

归档不含 PDB、环境文件、内部令牌、SMTP 凭据或外部私钥。

## 3. 自动与隔离验收

- API、契约、监控规则、内部令牌校验、管理页面和既有功能测试全部通过。
- 平台监控脚本语法、固定事件字段、合成端点和 transition-only 邮件逻辑通过。
- 隔离数据库应用迁移 17，告警激活、更新、恢复、清理和幂等写入通过。
- 错误 Host、未认证管理端点、无效内部令牌和超出时间窗口的事件均被拒绝。

## 4. 生产部署

最终切换前统一备份位于：

```text
/var/backups/hechao-unified-account/20260727T021850Z
```

数据库 dump SHA-256 为
`A804B8C8B24377FD5B0E5E13D70463691B1A4C42D0B0D303E070B46ED37F5D07`，
`pg_restore --list` 成功读取 `167` 个目录项；清单文件 SHA-256 为
`116F86AB1DA4D2C65D92DECE3E684C8FB23F3E816095AABD3FB471AA1927AFDC`。

原子切换后：

- `current` 指向
  `/opt/hechao-launcher-api/releases/0.20.0-20260727T011953Z`。
- 安装目录 PDB 数为 `0`，程序 SHA-256 与候选一致。
- 公网 `/healthz`、`/readyz` 返回 `200`、版本 `0.20.0`、database ready。
- 迁移最大值为 `17`，服务器和客户端档案数量均为 `6`。
- systemd `NRestarts=0`，部署后 journal 无 warning/error。
- 旧官网、中转 API、管理入口和私有 OSS 匿名 `403` 基线保持不变。
- 平台监控 timer 已启用，首次邮件与告警变化邮件送达。

本次没有启动、停止或重启 Minecraft、Velocity 或任何游戏服。

## 5. 回滚

直接回滚目标为 `0.19.0-20260727T005013Z`。迁移 17 与告警历史保留，旧 API 不读取
新增表。若独立监控器本身故障，先停用其 timer，API 的内部请求指标和游戏服告警仍可
继续评估。
