# API 0.23.1 候选发布记录

> 状态：修复候选已构建并验证；尚未部署
>
> 发布 ID：`0.23.1-20260730T113753Z`
>
> 制品源码提交：`d71d9e3bf70198044f19133f7747512791660a0e`
>
> 当前生产：`0.22.0-20260729T144953Z`
>
> 失败候选：`0.23.0-20260730T112557Z`，已回滚且禁止再次部署

## 1. 变更

保留 `0.23.0` 的结构化服控 API、后台第九个页面、冲突组编排、快捷设置、受限
Minecraft 控制台和默认关闭边界，并修复迁移 `020` 未加入
`DatabaseMigrator` 显式注册表的问题。

新增防回归测试会枚举程序集中的全部 SQL 资源，并要求：

- 注册版本从 `1` 连续到嵌入资源总数；
- 注册资源与按名称排序的全部嵌入 SQL 一一相等；
- 以后新增 SQL 但漏注册时，自动测试必须失败。

## 2. 制品

| 制品 | 大小 | SHA-256 |
| --- | ---: | --- |
| `hechao-api-0.23.1-20260730T113753Z.tar.gz` | `45,640,492` | `1748CD6C4A922C4AA4C34D47D68DAFAA895C85182A0BF58DC06F329583D482AE` |
| `Hechao.Api` | `104,591,923` | `1BD45AFA85EB04DA26AF7A09EB8D7BC2A3430A15DEA99CB4C3D5802263B612BA` |

归档为 Linux x64 自包含单文件发布，无 PDB、环境文件或凭据。

## 3. 自动验证

- API 测试 `211/211` 通过。
- Release 模式完整 .NET 解决方案 `447/447` 通过。
- 新测试确认迁移 `001` 至 `020` 全部嵌入且全部注册。
- `0.23.0` 失败候选已自动化之外执行受控回滚，生产重新确认 API `0.22.0`、
  数据库迁移 `019` 和 `ready`。

## 4. 部署与回滚

复用 `0.23.0` 部署前已验证的数据库备份和 API/配置完整回滚包，但仍需重新核对
哈希、当前生产版本和服控关闭状态。部署只允许重启 API；必须在切换后确认迁移
`020`、四张服控表、`ServerControl__Enabled=false`、公网回归和新增日志错误为零。

任一检查失败，立即把 `current` 切回
`/opt/hechao-launcher-api/releases/0.22.0-20260729T144953Z` 并重启 API。迁移
`020` 是加表与索引，旧程序会忽略；若 SQL 应用本身失败，事务会整体回滚。
