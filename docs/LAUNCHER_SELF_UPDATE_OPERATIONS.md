# 启动器自更新

## 范围

启动器登录赫朝账号后查询 `GET /v1/launcher/update`。API 只返回当前正式安装包的
版本、最低支持版本、长度、SHA-256、发行说明和短时私有 OSS 地址。客户端仅接受
HTTPS（本机测试可用 loopback HTTP），下载支持断点续传，并在启动安装程序前重新
核对长度与 SHA-256。

更新器是当前单文件启动器的临时副本。它等待主程序退出，以 NSIS 静默覆盖安装，
再从当前用户注册表读取安装目录并启动新版本。下载、校验、启动更新器或安装失败时，
已安装版本不会被提前删除；错误写入
`%LocalAppData%\Hechao\Launcher\updates\last-update-error.log`，并尽力重新启动
原安装版本。

首版不依赖 Authenticode。完整性边界是 HTTPS、已认证 API、私有 OSS 签名地址与
API 提供的 SHA-256；因此 API 的发布凭据和管理员会话仍属于高敏感资产。

## 发布顺序

1. 完整构建和测试新启动器。
2. 使用 `tools/Publish-PrivateLauncherRelease.ps1` 将安装包写入不可变对象：
   `releases/launcher/<version>/Hechao-Launcher-Setup-<version>-win-x64.exe`。
3. 用两次独立签名回读核对长度与 SHA-256。
4. 确认 API RAM 策略包含
   `oss:GetObject` 的 `hechaoworld/releases/launcher/*`，不能授予写权限。
5. 在 API 主机运行 `deploy/linux/configure-launcher-updates.sh`，写入：

```text
LauncherUpdates__Enabled=true
LauncherUpdates__LatestVersion=0.13.0
LauncherUpdates__MinimumSupportedVersion=0.12.3
LauncherUpdates__InstallerBytes=<bytes>
LauncherUpdates__InstallerSha256=<sha256>
LauncherUpdates__PublishedAt=<UTC ISO-8601>
LauncherUpdates__ReleaseNotes=<single-line notes>
```

## 首次启用边界

`launcher-v0.13.0` 是第一份包含自更新模块的启动器。仍在运行
`launcher-v0.12.3` 或更早版本的玩家必须手动覆盖安装一次 `0.13.0`；
旧程序本身没有 `/v1/launcher/update` 客户端，无法自行完成这次跨越。

`0.13.0` 和 `0.13.1` 因更新弹窗的只读进度属性绑定错误，在真实启动验收时
被撤回且未向玩家开放。修复后的首次过渡版是 `0.13.2`。

从 `0.13.2` 开始，后续版本通过启动器内的更新提示下载、校验、静默覆盖并
重新启动。发布首轮真实验收使用 `0.13.2 -> 0.13.3`，不得把
`0.12.3 -> 0.13.2` 记作自更新验收。

6. 重启 API，先用测试账号核对可选更新，再提高
   `MinimumSupportedVersion` 开启强制更新。

配置脚本会先保留带 UTC 时间戳的环境文件备份。发布失败时不要改
`LatestVersion`；API 或 OSS 故障时，启动器继续保留并运行当前版本。

## 0.14.0 自动检查与更新行为

启动器每次进程启动都会在恢复赫朝账号会话后检查一次正式更新通道。发现高于当前
版本的发布后，不再等待玩家点击“立即安装”，而是立即显示固定布局更新页、下载并
校验安装包，再启动独立 updater。updater 等待旧进程退出、静默覆盖安装并重新拉起
启动器；任一步失败都保留当前版本，并在更新页提供重试状态。

生产更新端点和安装包仍要求赫朝登录态，匿名用户不会取得私有 OSS 签名 URL。
没有可恢复会话时，检查会延后到本次进程登录或注册成功后立即执行；因此不会为了
“启动时检查”放宽内部下载权限。

验收至少覆盖：

1. 当前版本返回 `204`，不显示更新页。
2. 可选和强制更新都自动进入下载，不依赖按钮点击。
3. 下载大小和 SHA-256 校验后才允许 updater 启动。
4. 下载失败、签名 URL 过期或 updater 启动失败时，当前 EXE 不被替换并可重试。
5. 覆盖升级保留 `%LocalAppData%\Hechao\Launcher` 和游戏数据根目录。

## 0.14.2 生产快照

- `2026-08-01` 生产 `LatestVersion=0.14.2`，最低支持版本保持
  `0.12.3`，因此没有扩大强制更新范围。
- 安装包为 `61,929,723` 字节，SHA-256
  `D71A6BAED73FE1A9F503DDC7282A73B8F2E4C51145B6A8562133058DF401D6D8`。
- 私有对象为
  `releases/launcher/0.14.2/Hechao-Launcher-Setup-0.14.2-win-x64.exe`；
  两轮签名读取 `200`、两轮匿名读取 `403`，未记录签名 URL。
- 正式 `LauncherApiClient` 使用现有 DPAPI 会话取得 `0.14.2` 元数据；
  `0.14.1` 生成更新计划，`0.14.2` 不重复提示。API 签发地址完整下载的
  长度和 SHA-256 与发布制品一致。
- API 环境文件切换前备份为
  `/etc/hechao-launcher-api/environment.launcher-updates.20260801T091627Z.bak`；
  API `0.24.2` 保持原发布目录，只重启 API 服务，未操作游戏服务器。

完整记录见 [`LAUNCHER_RELEASE_0.14.2.md`](LAUNCHER_RELEASE_0.14.2.md)。

## 0.14.1 历史快照

- `2026-08-01` 已将生产通道切换为 `LatestVersion=0.14.1`，最低支持版本保持
  `0.12.3`。
- 安装包为 `61,912,187` 字节，SHA-256 为
  `6B31535E3A5CF08FF6D5F3D2B9CAD1B69DD5B3E3242959132DFDAD240476413E`。
- 私有对象为
  `releases/launcher/0.14.1/Hechao-Launcher-Setup-0.14.1-win-x64.exe`；两轮签名读取
  均为 `200`，匿名读取为 `403`，未记录签名 URL。
- 第一次切换因远程 stdin 破坏中文 UTF-8 发行说明而失败，部署流程已自动恢复
  `0.14.0` 与 API；第二次使用 ASCII 单行说明后切换成功。
- 管理机上的 `0.14.0` 正式进程未被关闭或替换。最终启动自动更新由用户亲自关闭并
  重新打开启动器验收，完成前保持“待验收”。

完整记录见 [`LAUNCHER_RELEASE_0.14.1.md`](LAUNCHER_RELEASE_0.14.1.md)。

## 0.14.0 历史快照

- `2026-07-31` 生产更新通道已指向 `0.14.0`，最低支持版本保持 `0.12.3`。
- 正式安装包为 `61,910,638` 字节，SHA-256
  `2F6ED3DBD94472DE99578DDCFA4CAFD504436575946722A6074A8F8C80AB0E8B`。
- 私有对象为
  `releases/launcher/0.14.0/Hechao-Launcher-Setup-0.14.0-win-x64.exe`；
  两轮签名读取均为 `200`，匿名读取为 `403`，未记录签名 URL。
- 已完成 `0.13.7 -> 0.14.0` 隔离覆盖安装、全新安装和双轮卸载，设置、会话与
  运行中启动器均按预期保留。
- 管理机已从 `0.13.6` 完成最后一次引导升级，全部状态文件哈希保持不变。
- `0.14.0` 是首个会在启动时自动下载并重启的版本；后续升级无需玩家重新安装。

## 0.13.7 历史快照

- `2026-07-31` 生产更新通道已指向 `0.13.7`，最低支持版本保持 `0.12.3`。
- 安装包为 `61,910,615` 字节，SHA-256 为
  `5E7DD73BED96BE98EEB616CCF8155D268B92C8A0A25B54BB4D67F0712429B5CF`。
- 私有对象为
  `releases/launcher/0.13.7/Hechao-Launcher-Setup-0.13.7-win-x64.exe`；
  两轮签名回读均为 `200`，匿名读取为 `403`，未记录签名 URL。
- 已完成 `0.13.6 -> 0.13.7` 覆盖安装、全新安装和双轮卸载，设置、会话与
  既有启动器进程均保留。
- 更新弹窗改为固定行布局，更新说明限高滚动，进度条和状态分行，按钮文字与图标
  颜色固定；125% DPI 预览未发现重叠。
- 该版本发布时正式机仍运行 `0.13.6`，当时的“下载并重启”验收为避免打断游戏而暂缓；该历史缺口已由 `0.14.0` 引导升级收口。

## 0.13.6 已验收快照

- `2026-07-31` 生产更新通道已指向 `0.13.6`，最低支持版本保持 `0.12.3`。
- 安装包为 `61,904,234` 字节，SHA-256 为
  `9A5F09BD5084C4C926598184A536825B4F59A0116571D255D4603F7CD54A4C03`。
- 私有对象为
  `releases/launcher/0.13.6/Hechao-Launcher-Setup-0.13.6-win-x64.exe`；
  两轮签名回读均为 `200`，匿名读取为 `403`，未记录签名 URL。
- 正式客户端已完成 `0.13.5 -> 0.13.6` 自更新，账号、皮肤、Java、目录、
  内存和设置均保留；更新错误日志不存在。
- 同一安装包已修复正式机残留的卸载项版本，最终 `DisplayVersion=0.13.6`。
- `0.13.5` 只作为内部验收构建保留，不创建标签、不覆盖对象、不作为回滚目标。
- `0.13.6` 新增系统代理开关，默认关闭；切换后下次启动生效。

## 回滚

回滚只需将 `LatestVersion` 和对应长度、SHA-256 改回上一份仍保留的不可变安装包，
或将 `LauncherUpdates__Enabled=false` 后重启 API。已经安装新版本的玩家不会被
自动降级；需要降级时发布更高版本号且内容基于上一稳定版本，避免版本比较倒退。

## 验收

- 当前版本请求返回 `204` 或无更新。
- 旧版本收到可选或强制更新提示，下载进度不阻塞 UI。
- 断网、截断安装包和错误 SHA-256 都不关闭当前版本。
- 安装中失败会留下错误日志并重新打开原版本。
- 成功更新后，设置、DPAPI 会话、客户端档案和游戏目录保持不变。

## 0.15.0 生产快照

- `2026-08-08` 生产 `LatestVersion=0.15.0`，最低支持版本保持 `0.12.3`。
- 安装包为 `61,960,670` 字节，SHA-256
  `35B2A08450E125ACC6EF096DEC4B3D768BF405CFAF2A7C20CF12FD1F52A38A96`。
- 私有对象首次上传成功，重复发布校验后跳过；两轮签名读取 `200`、两轮匿名读取
  `403`，签名 URL 未进入发布记录。
- 真实 DPAPI 会话确认 `0.14.2` 生成更新计划、`0.15.0` 不重复更新；API 签发地址
  完整下载的长度和 SHA-256 与安装包一致。
- API `0.29.0` 只重启自身，备份环境文件后原子切换；官网、后台、中转站、公开活动源
  和内外网健康检查均通过。

完整记录见 [`LAUNCHER_RELEASE_0.15.0.md`](LAUNCHER_RELEASE_0.15.0.md)。
- 匿名 OSS 请求返回 `403`，API RAM 只有读取两个批准前缀的权限。

## 0.15.1 生产快照

- `2026-08-08` 生产 `LatestVersion=0.15.1`，最低支持版本保持 `0.12.3`。
- 安装包为 `61,960,307` 字节，SHA-256
  `09CB52C4BEF63024345E4C99A0E355752D9082153F01274E298B807BAF93BEBF`。
- 私有对象首次上传成功，重复发布校验后跳过；两轮签名读取 `200`、匿名读取 `403`，
  签名 URL 未进入 Git、文档或终端记录。
- 真实 DPAPI 会话确认 `0.15.0` 生成更新计划、`0.15.1` 不重复更新；API 签发地址
  完整下载的长度和 SHA-256 与安装包一致。
- 第一次切换因验收脚本使用错误的公开元数据字段名而自动回滚到 `0.15.0`；修正为
  实际契约后重新切换成功，证明环境文件恢复和 API 自动重启路径有效。
- API 保持 `0.29.0`，常驻 Publisher Agent 保持 `1.2.1`；Minecraft、Velocity 和
  服控代理均未操作。

完整记录见 [`LAUNCHER_RELEASE_0.15.1.md`](LAUNCHER_RELEASE_0.15.1.md)。

## 0.15.2 生产快照

- `2026-08-09` 生产 `LatestVersion=0.15.2`，最低支持版本保持 `0.12.3`。
- 安装包为 `61,961,528` 字节，SHA-256
  `482BA9F5BE5CB3817B9AE39FD6C90C313B31DA809FCBE480F856EF31645A476F`。
- 私有对象首次上传成功，重复发布校验后跳过；两轮签名读取 `200`、匿名读取 `403`，
  签名 URL 未进入 Git、文档或终端记录。
- 真实 DPAPI 会话确认 `0.15.1` 生成更新计划、`0.15.2` 不重复更新；API 签发地址
  完整下载的长度和 SHA-256 与安装包一致。
- `0.15.1 -> 0.15.2` 隔离覆盖安装、全新安装、双轮卸载、设置与会话保留均通过。
- API 保持 `0.29.0`，常驻 Publisher Agent 保持 `1.2.1`；Minecraft、Velocity 和
  服控代理均未操作。

完整记录见 [`LAUNCHER_RELEASE_0.15.2.md`](LAUNCHER_RELEASE_0.15.2.md)。

## 0.15.3 生产快照

- `2026-08-09` 生产 `LatestVersion=0.15.3`，最低支持版本保持 `0.12.3`。
- 安装包为 `61,954,842` 字节，SHA-256
  `EC84277585366D8C1FF1B1AB60E89AA433BF142BDC26B924E772D88F52A271CE`。
- 私有对象首次上传成功，重复发布校验后跳过；两轮签名读取 `200`、匿名读取 `403`，
  签名 URL 未进入 Git、文档或终端记录，远端一次性暂存文件已清理。
- 真实 DPAPI 会话确认 `0.15.2` 生成更新计划、`0.15.3` 不重复更新；API 签发地址
  完整下载的长度和 SHA-256 与安装包一致。
- `0.15.2 -> 0.15.3` 隔离覆盖安装、全新安装、双轮卸载、设置与会话保留均通过。
- API 保持 `0.29.0`，常驻 Publisher Agent 保持 `1.2.1`；Minecraft、Velocity 和
  服控代理均未操作。

完整记录见 [`LAUNCHER_RELEASE_0.15.3.md`](LAUNCHER_RELEASE_0.15.3.md)。

## 0.15.5 生产快照

- `2026-08-10` 生产 `LatestVersion=0.15.5`，最低支持版本保持 `0.12.3`。
- 安装包为 `61,963,099` 字节，SHA-256
  `03724C0D97A32270103012426E891A55F088AE4470796C9415BB609E828DC5AF`。
- 私有对象首次上传成功，重复发布校验后跳过；两轮签名读取 `200`、匿名读取 `403`，
  签名 URL 未进入 Git、文档或终端记录，远端暂存和凭据挂载均已清理。
- 真实 DPAPI 会话确认 `0.15.4` 生成更新计划、`0.15.5` 不重复更新；API 签发入口
  完整下载的长度和 SHA-256 与安装包一致。
- `0.15.4 -> 0.15.5` 隔离覆盖安装、全新安装、双轮卸载、设置与会话保留均通过。
- 第一次切换因验收脚本错误要求公开元数据包含内部最低版本字段而自动回滚到
  `0.15.4`；修正为公开接口的真实脱敏契约后重新切换成功。
- API 保持 `0.29.0`，常驻 Publisher Agent 保持 `1.2.1`；Minecraft、Velocity 和
  服控代理均未操作。

完整记录见 [`LAUNCHER_RELEASE_0.15.5.md`](LAUNCHER_RELEASE_0.15.5.md)。

## 0.15.6 生产快照

- `2026-08-10` 生产 `LatestVersion=0.15.6`，最低支持版本保持 `0.12.3`。
- 安装包为 `61,962,345` 字节，SHA-256
  `00C5C21CE8ABEA2FB15DA49DFB2CD5BA267E582DE49DBF96D91EF938C41822B3`。
- 私有对象首次上传成功，重复发布校验后跳过；两轮签名读取 `200`、匿名读取 `403`，
  签名 URL 未进入 Git、文档或终端记录，远端暂存和瞬态凭据挂载均已清理。
- 真实 DPAPI 会话确认 `0.15.5` 生成更新计划、`0.15.6` 不重复更新；API 签发入口
  完整下载的长度和 SHA-256 与安装包一致。
- `0.15.5 -> 0.15.6` 隔离覆盖安装、全新安装、双轮卸载、设置与会话保留均通过。
- 更新元数据切换一次成功；自动回滚门禁已启用但未触发。API 保持 `0.29.0`，常驻
  Publisher Agent 保持 `1.2.1`；Minecraft、Velocity 和服控代理均未操作。
- 切换前已运行的本机 `0.15.5` 实例没有被强制替换，将在后续启动并恢复登录态后
  按正常自更新流程升级。

完整记录见 [`LAUNCHER_RELEASE_0.15.6.md`](LAUNCHER_RELEASE_0.15.6.md)。
