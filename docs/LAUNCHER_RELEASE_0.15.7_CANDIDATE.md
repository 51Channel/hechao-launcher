# 赫朝启动器 0.15.7 候选发布记录

- 候选日期：2026-08-10
- 上一正式版本：`0.15.6`
- 源码基线：`837a4434a7576411cf294409ea9ca7b0ff9859be`
- 候选分支：`codex/launcher-0.15.7-sidebar-account-cleanup`
- 正式状态：候选未发布；没有生成或上传安装包，也没有切换生产更新通道。

## 候选范围

1. 删除左下账户卡右侧与“账户”导航重复的独立账号操作按钮。
2. 同时删除按钮预留列和标题右侧空白，让账号名称与身份信息使用完整可用宽度。
3. “账户”导航、账户页登录与退出、设置页账号操作及管理员后台入口保持不变。

## 候选门槛

- [x] 账号区域 XAML 契约测试通过，并新增“账户卡内不允许重复操作按钮”的回归约束。
- [x] 启动器测试 `225/225`、完整解决方案测试 `710/710` 通过。
- [x] `1590 x 960`、`1673 x 960` 与 `2250 x 1290` 三档真实 WPF 截图无重叠、裁切
  或异常留白，测试前后启动器设置逐字节一致。
- [x] Release 单文件候选构建成功，`git diff --check` 通过。

## 发布边界

- 当前正式版本仍为 `0.15.6`；本候选未修改版本号、OSS、Launcher API 或更新元数据。
- 管理后台仍要求由同一台电脑上的启动器签发一次性管理员票据，票据来源网络地址校验
  不变；不能通过直接收藏 `/admin` 深层地址绕过登录。
- 不修改服务器目录、客户端档案、游戏启动状态机或 Minecraft 数据。
- 不启动、停止或重启 Minecraft、Velocity、服控代理、API 或 Publisher。

## 回滚

在正式发布前直接放弃本候选分支即可。正式发布后若仅需撤销界面变化，应使用更高版本
恢复账户卡按钮；不得覆盖 `0.15.6` 或任何既有正式 OSS 对象与 Git 标签。
