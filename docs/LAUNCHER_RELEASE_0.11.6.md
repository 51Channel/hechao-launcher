# 赫朝启动器 0.11.6 发布记录

> 构建日期：`2026-07-26`
>
> 启动器源码提交：`6998dc344c40b49eda0137bb239f3ddd058d248f`
>
> 配套 API：`0.11.1-20260725T165050Z`，本次没有服务端变更
>
> 状态：本机内部候选已验证；安装包尚未上传 OSS 或面向玩家发布

## 修复范围

`0.11.5` 将账号页签改为与游戏内存选项相同的等宽完整矩形，但页签标题的居中属性同时作用到了选中内容，导致登录和注册表单按最小宽度排列。该版本只在本机安装检查，未上传、未打标签，也未向玩家分发。

`0.11.6` 将标题与内容对齐分开处理：

- 登录和注册标题继续在两个等宽按钮中居中显示。
- 两个按钮均使用完整四边框，选中状态为浅红底与红色边框。
- 选中内容宿主明确横向、纵向拉伸。
- 登录输入框、密码框和主按钮恢复整栏宽度。
- 注册页用户名、显示名称、邮箱、验证码、密码和主按钮恢复原有网格布局。

## 验收证据

- 真实 WPF 渲染测试覆盖登录和注册两种选中状态。
- 测试确认两个页签等宽、边框与可见按钮外沿重合，左右竖边间距等于按钮物理宽度。
- 新增表单回归断言，选中内容宽度必须等于 `TabControl` 整栏宽度，不能退回内容最小宽度。
- 启动器测试 `57/57` 通过；完整解决方案及正式安装器构建均为 `193/193` 通过。
- 已从本机失败候选 `0.11.5` 静默覆盖安装到 `0.11.6`，设置和既有客户端目录保留。
- 正式安装版在 1500 x 860 窗口分别完成登录页与注册页实机检查，输入区域和主按钮均完整铺开。

## 制品

| 项目 | 值 |
| --- | --- |
| 启动器 EXE | `D:\Hechao Launcher\Hechao.Launcher.exe` |
| EXE ProductVersion | `0.11.6+6998dc344c40b49eda0137bb239f3ddd058d248f` |
| EXE 大小 | `68,637,418` 字节 |
| EXE SHA-256 | `58C1063B5BC65684C55FC685DCB0E5F45CD4151B3DAC8E361030D0BFF1A59F67` |
| 安装包 | `artifacts/installer/Hechao-Launcher-Setup-0.11.6-win-x64.exe` |
| 安装包大小 | `61,802,610` 字节 |
| 安装包 SHA-256 | `32E06CF9DCE0811293E1279C4C76B8B2C5C8401859FC5A84DCE64AB1227416E9` |
| Windows 签名 | EXE 与安装包均为 `NotSigned` |

## 边界

本次只修改 Windows 启动器账号页签布局和对应测试。没有变更 API、数据库、OSS 对象、客户端档案、Velocity 或任何 Minecraft 服务器；没有调用发布器、上传安装包或生成玩家下载链接。
