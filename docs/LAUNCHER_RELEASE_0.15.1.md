# 赫朝启动器 0.15.1 发布记录

- 发布日期：2026-08-08
- 功能来源提交：`5d1d80f346d49479c2336c14b03524bdad4a2044`
- 发布准备提交：`d1a4cf2601f2e43f45fd69f870682b6c202acfcf`
- 正式标签：`launcher-v0.15.1`
- 生产通道切换时间：`2026-08-08T10:25:59Z`

## 变更内容

1. 服务器主页改为宽导航、真实服务器目录和当前服务器主视区组成的三栏布局。
2. 新增真实公告与近期活动；只投影玩家可见目录中的真实数据，并提供明确空状态。
3. Java、内存和游戏目录进入首屏快捷设置，完整客户端维护能力继续保留。
4. 官网同源活动月历完整保留，因此本安装包同时包含固定六周月历与新版主页。
5. 活动客户端下载、自更新、目录过滤和游戏启动状态机没有改变。

## 构建与测试

| 制品 | 字节 | SHA-256 | 版本 | 签名 |
| --- | ---: | --- | --- | --- |
| `Hechao-Launcher-Setup-0.15.1-win-x64.exe` | `61,960,307` | `09CB52C4BEF63024345E4C99A0E355752D9082153F01274E298B807BAF93BEBF` | `0.15.1` | `NotSigned` |
| `Hechao.Launcher.exe` | `69,018,905` | `3A3D642988A52F336D2FAB7218E9AFC1D7F1ED5635002593E689DBF62F23284C` | `0.15.1+d1a4cf2601f2e43f45fd69f870682b6c202acfcf` | `NotSigned` |

- Release 完整解决方案测试：`707/707`，其中 Launcher `222/222`、Publisher `55/55`。
- PowerShell 7 合规与 18 项活动发布溯源检查通过。
- `1500 x 860` 与 `1060 x 640` 真实 WPF 截图通过，无横向溢出、重叠或主要操作裁切。
- 隔离安装验收：`0.15.0 -> 0.15.1`、全新安装和两轮卸载均通过；设置、会话与
  既有正式启动器进程均保留。

## 私有 OSS

- 不可变对象：
  `releases/launcher/0.15.1/Hechao-Launcher-Setup-0.15.1-win-x64.exe`。
- Publisher CLI `1.3.0` 在独立一次性 systemd 单元中首次上传；第二次核对长度、元数据
  和 SHA-256 后跳过覆盖。
- 两轮独立签名下载均为 `200`，长度和 SHA-256 与正式安装包一致；匿名读取为 `403`。
- 签名 URL 只存在于远端 `0700` 临时目录，验收后连同暂存制品精确删除。

## 生产更新通道

- `LatestVersion=0.15.1`
- `MinimumSupportedVersion=0.12.3`
- `InstallerBytes=61960307`
- `InstallerSha256=09cb52c4bef63024345e4c99a0e355752d9082153f01274e298b807baf93bebf`
- API 保持 `0.29.0` 与原发布目录，只重启 `hechao-launcher-api.service`；最终 PID
  `3088692`、`NRestarts=0`，切换后错误级日志为 `0`。
- 环境文件保持 `root:root 600`；正式切换前备份为
  `/etc/hechao-launcher-api/environment.launcher-updates.20260808T102559Z.bak`。
- 内外网健康/就绪、官网、后台、公开活动与公开下载入口均通过；公开活动当前为 `0` 条。

第一次切换后的发布物本身正常，但验收脚本错误读取公开元数据的 `bytes` 字段；实际契约为
`installerBytes`。事务因此自动恢复 `0.15.0` 并重启 API。修正验收字段后第二次切换成功，
回滚路径得到一次真实验证。

## 真实更新链验收

- 现有 DPAPI 会话恢复成功，生产 API 返回 `0.15.1`、最低版本 `0.12.3`、正确长度与
  SHA-256。
- `0.15.0` 生成更新计划，`0.15.1` 不生成重复更新计划。
- API 签发地址完整下载返回 `200`，共 `61,960,307` 字节，SHA-256 与发布制品一致。
- 验收工具未输出账号身份、会话令牌或签名 URL，正式启动器进程未被关闭或替换。

## 运行边界与回滚

常驻 Publisher Agent 保持 `1.2.1`、PID `1459607` 和 `NRestarts=0`；本次没有启动、停止
或重启 Minecraft、Velocity、服控代理或任何游戏服务。若分发异常，恢复上述 API 环境
备份并只重启 Launcher API，或将 `LauncherUpdates__Enabled=false`。已经安装 `0.15.1`
的客户端不会自动降级。

结构化证据见
[`evidence/LAUNCHER_0.15.1_RELEASE_ACCEPTANCE_2026-08-08.json`](evidence/LAUNCHER_0.15.1_RELEASE_ACCEPTANCE_2026-08-08.json)。
