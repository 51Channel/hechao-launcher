# 赫朝启动器 0.15.5 候选发布记录

- 候选日期：2026-08-09
- 最近校正：2026-08-10
- 上一正式版本：`0.15.4`
- 源码基线：`cb1d7e957ceda19677a0994e255c1f953d4ad6b8`
- 正式构建源码提交：`6c71303959b89861316405541a3693f6f9900be5`
- 正式标签：`launcher-v0.15.5`
- 已替代方案：`310c937` 的固定高度、目录收口和账户上移布局不再有效。
- 正式状态：已于 `2026-08-09T18:11:26Z` 切换生产更新通道；正式记录见
  [`LAUNCHER_RELEASE_0.15.5.md`](LAUNCHER_RELEASE_0.15.5.md)。

## 候选范围

1. 删除侧栏底部社区声明，账户卡继续作为底部锚点，并与目录和快捷设置统一底线。
2. 服务器目录保持贯穿业务区，列表使用剩余高度并在内部滚动；目录左右两侧留白统一
   为 `14`。
3. 当前服务器、公告/近期活动和快捷设置使用带上下限的弹性比例，删除独立底部填充行，
   让主页业务卡共同填满可用高度。
4. 当前服务器详情整体垂直居中，主操作紧跟详情信息，并保留 `40` 像素最小高度避免
   按钮底边裁切。
5. 快捷设置重做为居中的紧凑工具条，Java、运行内存和当前 `.minecraft` 使用浅灰字段
   表面；所有原有绑定、命令、路径省略和打开目录能力保持不变。

## 候选门槛

- [x] XAML 解析和启动器契约测试通过。
- [x] 启动器测试 `225/225`、完整解决方案测试 `710/710`，Release 构建 `0` 警告、
  `0` 错误。
- [x] `1590 x 960`、`1673 x 960` 和 `2250 x 1290` 三档真实 WPF 截图无重叠、
  裁切、异常拉伸或路径溢出，三处底边及目录两侧留白均对齐。
- [x] Taste 人工审查、Impeccable 布局扫描和 `git diff --check` 通过。
- [x] 用户确认后从固定源码提交重新构建正式安装包，产品版本、长度、SHA-256 和
  `NotSigned` 状态均已核对。
- [x] `0.15.4 -> 0.15.5` 覆盖安装、全新安装、双轮卸载及设置和会话保留通过。
- [x] 私有 OSS 首次上传、重复发布校验后跳过、两轮签名回读和匿名拒绝通过。
- [x] 生产切换和真实登录态更新链通过；`0.15.4` 生成更新计划，`0.15.5` 不重复更新。

## 发布边界

- 不修改服务器目录数据、客户端档案、开放状态、公告、活动或游戏启动状态机。
- 不启动、停止或重启 Minecraft、Velocity、服控代理或 Publisher。
- `0.15.4` 的 OSS 对象与 Git 标签保持不可变；任何正式修复只能发布为更高版本。

## 回滚

若分发异常，恢复
`/etc/hechao-launcher-api/environment.launcher-updates.20260809T181126Z.bak` 并只重启
Launcher API，或关闭 `LauncherUpdates__Enabled`。已经安装 `0.15.5` 的客户端不会
自动降级；禁止覆盖 `0.15.4`、`0.15.5` 的安装包或标签。
