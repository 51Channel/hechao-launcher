# 天域远征工业季客户端档案 1.0.15 Test 发布

- 发布日期：2026-08-18
- 档案 ID：`skyrealm-industrial-neoforge-1.21.1`
- 不可变版本：`1.0.15`
- 清单 SHA-256：
  `C32A8BC5B945B0FDEBC1349AD836D911CA308E91987CDD540E1FBFFBCB59D129`
- Test：`r8 / 1.0.15 / 100%`
- Gray：未分配，`r1`
- Production：未分配，`r1`

## 发布结果

- `1.0.14` 与 `1.0.15` 的 `4,456` 个共同文件路径、大小、SHA-256 和对象 URL 全部一致；
- 删除 Screen `0.1.5`，新增 Screen `0.1.6`，同路径内容变化为 `0`；
- 逻辑文件 `4,457`，逻辑大小 `1,203,314,577` 字节；
- 去重对象 `4,252`，对象大小 `1,203,306,067` 字节；
- OSS 对 `4,251` 个既有对象完成复核，仅上传 `1` 个新对象、`33,099` 字节；
- 匿名后台和对象 API 请求均返回 `401`。

生产 API 已按原始字节保存签名清单，远端与本地大小均为 `2,025,517` 字节，回读
SHA-256 与本地一致。远端清单权限为 `0640`、所有者为 `hechao-api:hechao-api`。
后台审计分别记录签名版本导入和 Test 通道更新；API `0.33.1` 健康、就绪、服务状态
与 `NRestarts=0` 均正常，近期 warning 以上日志为 `0`。没有操作任何 Minecraft、
Velocity、API、代理或服控进程。

## 回滚与剩余验收

Test 回滚目标为 `1.0.14` 清单
`F5EB933E865E6F7F4B8C1916FB42319173A482399816E7C8D15C726BE56EF481`。回滚只移动
Test 指针，不删除 `1.0.15` 清单和对象。

玩家正常退出旧游戏后，由启动器执行增量更新并重新启动，再验收六个动作是否先显示即时
反馈，并继续获得正确的服务端结果。完成真人验证前不得推进 Gray 或 Production。
