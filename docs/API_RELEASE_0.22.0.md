# API 0.22.0 正式发布记录

> 状态：生产运行中
>
> 发布 ID：`0.22.0-20260729T144953Z`
>
> 正式标签：`api-v0.22.0`
>
> 制品源码提交：`ba9576cd525de78fa639453e54466d967d5f1541`

## 1. 生产变更

- 迁移 `019` 增加 `Player | Infrastructure` 服务器角色和
  `monitoring_enabled`。
- 历史 `lobby` 记录固定为 `Infrastructure`、不可见、不可授权、不可协议转换，但
  继续采集心跳、TPS、MSPT、GC 和告警。
- 玩家目录、档案资格、一次性进服授权、Velocity 目标解析和单服规则只接受
  `Player` 角色。
- 数据库约束阻止大厅被普通管理操作重新公开。

## 2. 制品与部署

| 制品 | 大小 | SHA-256 |
| --- | ---: | --- |
| `hechao-api-0.22.0-20260729T144953Z.tar.gz` | `45,576,531` | `2C70353CDB1C9458ADF1B14DB358CB13A31B91ECB22AC25267FF824AE4058B03` |
| `Hechao.Api` | `104,453,683` | `CCD8EFAF4D1F3F89A1BF7C08F2F407283892F3CC69733155ACA6884D45073A13` |

- 生产目录：
  `/opt/hechao-launcher-api/releases/0.22.0-20260729T144953Z`
- 发布前备份：
  `/var/backups/hechao-launcher/api-pre-0.22.0-20260729T150635Z`
- 发布前数据库 SHA-256：
  `CDA327652BBA24F700659A0BA5C6AC2B8DC08D02A056FDFEA035020B35113D8B`
- 发布前实际生产版本为 API `0.21.0`、迁移 `018`。旧候选文档中把生产版本写成
  `0.20.2` 的描述仅为发布前计划，不作为实际回滚事实。

## 3. 生产验收

- 原子切换完成，数据库连续迁移到 `019`。
- `GET /healthz` 返回 API `0.22.0`、`status=ok`。
- `GET /readyz` 返回 API `0.22.0`、`database=ready`。
- 数据库大厅记录为 `Infrastructure`、不可见、不可授权、监控启用。
- 匿名公开目录只返回五个玩家服务器，`lobby` 为零命中。
- 旧网站、中转 API、启动器 API、管理域名和论坛桥回归通过。
- 发布后 journal 新增错误为 `0`。

## 4. 回滚

程序回滚使用发布前完整备份和旧程序归档；旧程序归档 SHA-256 为
`38A102C3C2B4F2ABB48F9DAE9CD5AA1C53D95AC10789DE420EA1899715A5E2C2`。
迁移 `019` 为兼容性加列和保护约束，优先保留数据库现状并仅回滚程序。无论是否回滚，
大厅都必须保持基础设施角色、隐藏、不可授权和可监控。
