# 天域远征工业季客户端档案 1.0.19 候选

- 候选日期：2026-08-18
- 档案 ID：`skyrealm-industrial-neoforge-1.21.1`
- Minecraft：`1.21.1`
- NeoForge：`21.1.228`
- Java：`21`
- 候选状态：已生成、验签、完成对象闭合并上传缺失对象
- 清单大小：`2,025,516` 字节
- 清单 SHA-256：
  `ABC8B15B512686D35981A378B9D0A92B4AD5A64EE0C290F8865158A8FC470528`

## 精确差异

候选从不可变 `1.0.18` 客户端源隔离制作，不读取或修改正在运行的游戏目录。

- 两个档案均为 `4,457` 个文件；
- `4,456` 个共同文件的路径、大小和 SHA-256 全部不变；
- 删除 `mods/HechaoEconomyScreen-NeoForge-1.21.1-0.1.9.jar`；
- 新增 `mods/HechaoEconomyScreen-NeoForge-1.21.1-0.1.10.jar`；
- 同路径内容变化：`0`；
- 逻辑大小：`1,204,122,917` 字节；
- 去重对象：`4,252`，对象大小：`1,204,114,407` 字节；
- `4,251` 个对象与 `1.0.18` 相同，只新增 `1` 个 `841,439` 字节对象；
- 新 Screen JAR SHA-256：
  `02FC9FE719A103AAC1FCC0270560D2DD19EC9C51B8E12BD8017E08E35A5A2468`。

## 验证与发布门禁

- 正式信任包验签：通过；
- 全部对象大小、SHA-256、URL 和无多余对象闭合校验：通过；
- Gradle `clean test build` 连续两次通过，测试为 `32/32`；
- 后台只允许导入为不可变 `1.0.19`，先进入 `Test=100%`；
- Test 目标修订为 `r12`，Gray 与 Production 必须保持未分配；
- 当前运行客户端不得原地覆盖 JAR，必须退出后由启动器完成原子版本切换。

若 Test 出现回归，将 Test 通道回退到 `1.0.18` 清单
`9BE857DAEAD9743D79C96F04917E4040B5796153A9BA5C91826E3B51809562EB`；不删除或覆盖
`1.0.19` 对象和清单。候选发布与真人验收不启动、停止或重启任何 Minecraft、Velocity、
API、代理或服控进程。

