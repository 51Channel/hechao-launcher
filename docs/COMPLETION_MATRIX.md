# 赫朝启动器完成矩阵

> 更新时间：`2026-07-27`
>
> 启动器：`0.11.14`（私有 OSS 灰度候选）
>
> API：`0.20.1-20260727T145451Z`（生产在线）
>
> Velocity 授权插件：`0.2.0`，当前模式 `monitor`

本文档是“功能是否完成”的权威入口。其他计划和发布记录提供设计、操作与历史证据；
状态冲突时先核对本矩阵引用的代码、测试、制品和生产回归，再同步旧文档。

## 状态定义

| 状态 | 含义 |
| --- | --- |
| 已完成 | 代码、自动测试和要求范围内的制品或生产证据均存在 |
| 已实现待生产验收 | 代码和测试完成，但真实账号、真实服务或灰度证据不足 |
| 进行中 | 已开始实施，尚未达到完整验收条件 |
| 未实现 | 代码或必要管理界面不存在 |
| 外部验收 | 需要管理员、真实玩家或计划任务在真实环境完成，不能用夹具代替 |

## 1. 当前发布

| 项目 | 状态 | 证据或剩余条件 |
| --- | --- | --- |
| 启动器 `0.11.14` 源码与标签 | 已完成 | 制品源码提交 `6f337f337e15e4f7151d5df8a04db5fd40df98a7`，标签 `launcher-v0.11.14` |
| Windows 安装、覆盖升级与卸载 | 已完成 | 可重复验证脚本完成隔离目录干净安装、`0.11.13 -> 0.11.14`、双轮卸载、设置与 DPAPI 会话哈希、ProductVersion 和快捷方式验证 |
| 私有 OSS 发布 | 已完成 | 不可变对象、二次跳过、匿名 `403`、签名下载 `200`、`61,866,744` 字节和 SHA-256 复验 |
| 自动测试 | 已完成 | .NET `355/355`，Velocity `11/11`，LuckPerms 等级代理 `4/4`，Paper/Purpur 指标代理 `2/2`；私有重定向日志边界及 RAM 策略字段、动作和五个对象前缀的精确最小权限断言均已覆盖 |
| 2 至 3 人真实灰度 | 外部验收 | 按 [`PRELAUNCH_PILOT_0.11.14.md`](PRELAUNCH_PILOT_0.11.14.md) 执行 |
| 5 人与 20 人灰度 | 外部验收 | 前一档无阻断后逐级开放 |

## 2. 玩家端

| 能力 | 状态 | 证据或剩余条件 |
| --- | --- | --- |
| 赫朝账号与论坛统一账号 | 已完成 | 注册、登录、资料和密码内部接口已部署 |
| Microsoft/Minecraft 正版绑定 | 已实现待生产验收 | API 许可已通过；需要四级真实账号验收 |
| LuckPerms 等级同步、受控修改与目录过滤 | 已实现待生产验收 | API `0.17.0` 与代理文件已部署，强制目录开关未启用；等待服主下次自行重启大厅后做真实改回测试 |
| 签名清单、断点续传、修复与原子切换 | 已完成 | 基础、Vanilla、Forge、NeoForge、PVP、DollNight 档案与自动测试 |
| 启动时客户端检查开关 | 已完成 | `0.11.14` 可跳过首次本地扫描，重新开启立即检查；第一次进服前仍强制检查，自动测试覆盖 |
| 每档案 `.minecraft` 和 Java 17/21 | 已完成 | 自动 Java、自定义 Java、特殊路径别名和真实进程构建 |
| 安装失败自动恢复当前版本 | 已完成 | 原子切换与失败回滚测试 |
| 玩家主动回滚到上一版本 | 已完成 | 同锁安全克隆、玩家数据保留、确认界面、运行中保护、Java 复验及失败恢复测试 |
| Fabric 启动 | 已完成 | 基础与 PVP 档案 |
| NeoForge 启动 | 已完成 | 活动档案 `21.11.42` |
| 纯 Vanilla 档案 | 已完成 | `vanilla-1.21.11` / `1.0.0` 已签名发布并绑定 Survival1；完整安装、逐文件校验、进程构建和生产下载回归通过 |
| Forge 档案 | 已完成 | `forge-1.20.1` / `1.0.0` 已签名发布；完整安装、逐文件校验和 Forge `47.4.0` 进程构建通过，等待真实 Forge 服务器后再绑定 |
| DollNight 正式档案 | 已完成 | `dollnight-1.21.11` / `1.0.0` 已签名发布并绑定 DollNight；完整安装、逐文件校验、进程构建和分级授权回归通过 |
| 服务器在线、档案与进服授权检查 | 已完成 | UI 只允许在线目标启动；启动前重新申请一次性 Velocity 授权 |
| 本地退出记录与脱敏诊断包 | 已完成 | 固定条目、大小限制、路径脱敏和保留上限有测试 |
| 玩家确认后的诊断上传 | 已完成 | `0.11.14` 已由真实已登录客户端生成并确认上传编号 `1e707520`；上传端、生产端与管理员下载文件均为 `707` 字节且 SHA-256 一致，授权、完成和 `diagnostic.admin.downloaded` 审计及 14 天到期时间均已核对 |

## 3. 管理后台

| 能力 | 状态 | 证据或剩余条件 |
| --- | --- | --- |
| 管理员独立会话、CSRF、TOTP 与恢复码 | 已完成 | 真实管理员已于 2026-07-27 完成首次登记；生产凭据 `1`、恢复码哈希 `8`、未完成登记 `0`、有效 MFA 会话 `1`，并存在登记开始和启用审计 |
| 服务器新增、编辑、归档、恢复 | 已完成 | API、Web、修订号冲突和事务审计均已部署 |
| 显示名、短名、图标、排序、状态、人数、版本、加载器、最低等级 | 已完成 | `AdminServerCreateRequest` / `AdminServerUpdateRequest` 与 Web 表单 |
| 客户端档案与 Velocity 目标绑定 | 已完成 | 只允许绑定现有启用档案 |
| 维护公告与开放时间 | 已完成 | API `0.15.0`、迁移 10、玩家目录、Velocity 授权和 Web 表单均已在线；手动维护/关闭优先于排期 |
| 单服白名单、拒绝规则与有效期 | 已完成 | 允许/拒绝、原因、UTC 有效期、修订冲突与事务审计均已上线 |
| 玩家实际目录/进服结果预览 | 已完成 | 管理员可按玩家预览每个服务器的最终允许结果及拒绝原因 |
| 客户端档案创建、上传、签名与发布通道 | 已实现待生产验收 | API `0.17.0` 已生产部署，隔离生产副本真实验签与三通道闭环通过；真实管理员 MFA 已登记，仍待在生产管理页面完成创建、上传和发布操作验收 |
| 下载量、失败率与版本分布 | 已实现待生产验收 | API `0.20.1` 保留迁移 15、30 天留存、幂等批次和后台“运行数据”，并对客户端下载失败率建立统一告警；生产已收到一条 `0.11.14` 的 `LauncherStarted/Success` 真实事件，仍需安装、修复、启动等更完整样本和后台三个窗口页面验收 |
| 暂停问题版本和主动回滚 | 已实现待生产验收 | API `0.17.0` 已完成暂停自动回滚、恢复不自动推广、修订冲突和审计；真实管理员 MFA 已登记，仍待在生产管理页面完成暂停与回滚操作验收 |
| 玩家搜索、等级和单服授权管理 | 已实现待生产验收 | 玩家搜索、单服规则和受控四级全局等级入口均已上线；代理已部署但尚未随大厅手动重启加载 |
| 账号、设备会话与 Minecraft UUID 封禁 | 已实现待生产验收 | 功能自 API `0.16.0` 上线并保留在当前 `0.20.1`；账号停用/恢复、单设备和全部会话撤销、UUID 定时封禁、论坛既有 Cookie 联动、并发保护、Velocity/目录拒绝及审计均通过隔离与生产链路验收。真实管理员 MFA 已登记，仍待在生产页面逐项操作验收 |
| 审计日志查看 | 已完成 | 目录写入、登录与 MFA 事件可查询 |

## 4. 服务端授权与状态

| 能力 | 状态 | 证据或剩余条件 |
| --- | --- | --- |
| Velocity 异步授权和首次目标定向 | 已实现待生产验收 | 插件 `0.2.0` 为 `monitor`，合成 `lobby -> pvp` 通过 |
| NPC、`/hub`、断线重连和 API 故障路径 | 外部验收 | 需要真实玩家与真实代理日志 |
| Velocity `enforce` | 外部验收 | 四级账号与全部转服路径通过后切换 |
| 目录强制登录 | 外部验收 | `enforce` 稳定后启用 `Authentication__EnforceCatalogAuthentication=true` |
| 在线、人数、协议和软件版本心跳 | 已完成 | Windows 只读采集器每分钟上报 |
| TPS、MSPT、进程内存和启动时间 | 已实现待生产验收 | API `0.19.0` 与采集器 `0.2.0` 已生产上线，进程内存、CPU、启动时间和磁盘已实时入库；Paper/Purpur 代理 `0.1.0` 已部署到大厅、Survival1、Survival2，等待服主下次自行重启后加载并验证 TPS/MSPT/GC。活动 NeoForge 与远端 PVP 暂无深度指标 |
| 管理后台状态与错误摘要 | 已实现待生产验收 | “服务状态”和“运行告警”页面、30 天样本、24 小时固定问题摘要与告警历史保留在 API `0.20.1`，真实管理员 MFA 已登记；仍待用生产会话完成页面、样本和告警详情验收 |
| 远程启动、停止、重启 | 不在首版范围 | 若增加，必须使用最小权限代理、命令白名单、双确认和审计 |

## 5. 运维与正式上线

| 项目 | 状态 | 证据或剩余条件 |
| --- | --- | --- |
| API、数据库、HTTPS 与旧业务兼容 | 已完成 | API `0.20.1` 公网、管理域名和旧官网/中转 API 回归 |
| API 与 Nginx 敏感 URL 日志脱敏 | 已完成 | 私有 OSS 签名 URL 不再进入 API journal；五个 Nginx server block 不记录查询字符串或 Referer，真实签名下载及合成论坛重置 token 的新增日志命中均为 `0` |
| 数据库每日备份 | 已完成 | 本机恢复演练已做 |
| 数据库异地副本与异地主机恢复 | 已完成 | RAM v4 最小权限已默认生效；真实 OSS 上传、立即下载复验、定时任务、告警恢复、异地主机解密和隔离恢复均通过 |
| 论坛与 Sub2API 每日及异地备份 | 已实现待生产验收 | 在线 SQLite 快照、PostgreSQL custom dump、root-only 包、systemd 沙箱和 77 表隔离恢复均通过；RAM v5 策略边界、线上文件哈希、脚本语法、权限、disabled timer 和当前前缀 `403 AccessDenied` 已预检，等待明确授权后的真实 OSS 往返、异地主机恢复、定时器和告警验收 |
| Minecraft 世界串行原子备份 | 已实现待生产验收 | VSS 锁文件读取、后台 ZIP、条目、SHA、状态与卷影清理闭环通过；当前 Java 进程早于新脚本部署，正式归档数仍为 `0`，等待服主下一次自行重启后加载计划并验收 |
| 世界恢复演练 | 外部验收 | 必须从正式归档恢复到隔离目录并核对 |
| API、登录、下载和证书告警 | 已完成 | API `0.20.1`、迁移 17、平台监控器 `0.1.0`、后台告警页和 transition-only 邮件已生产验收 |
| TPS/MSPT/GC 与 20 至 30 人容量测试 | 外部验收 | 需要活动窗口和真实玩家 |
| 第二台 VPS `owl9` 管理基线 | 外部验收 | 当前密钥认证不可用 |
| 生产签名私钥离机恢复副本 | 已完成 | 发布器 `0.9.0` 恢复演练已通过；加密恢复包已写入私有 OSS 恢复前缀并完成回读逐字节复验 |
| Authenticode | 明确不做 | 首版保持 `NotSigned`，公告提供来源、大小和 SHA-256 |

## 6. 完成顺序

1. [已完成] 完成玩家主动回滚，并发布新的启动器候选。
2. [已完成] 管理员 MFA、真实客户端诊断上传、管理员下载、三段 SHA-256 一致性和全部对应审计均已核对。
3. [已完成] 发布 Vanilla、Forge 与 DollNight 正式签名档案。
4. [已实现待生产验收] 目录高级规则、玩家搜索、访问预览、单服权限、平台账号/UUID 安全、论坛既有会话联动、受控全局等级及客户端三通道发布已生产部署；真实管理员 MFA 已登记，仍待各管理页面和大厅代理加载验收。
5. [已实现待生产验收] 下载/失败/版本遥测、服务器进程/磁盘指标、统一告警及数据库异地备份恢复均已生产上线；TPS/MSPT 等待下次计划重启加载代理。
6. 使用四级真实账号完成 `monitor` 灰度。
7. 验证 NPC、`/hub`、断线重连和 API 故障，切换 Velocity `enforce`。
8. 启用目录强制登录，完成 5 人和 20 人灰度及真实回滚。
9. 一个观察周期无阻断故障后，形成正式发布公告和最终审计记录。
