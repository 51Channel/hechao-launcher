# 赫朝启动器 0.15.6 候选发布记录

- 候选日期：2026-08-10
- 上一正式版本：`0.15.5`
- 源码基线：`69b58358d2a25ca5f4a0905c56278ef19fa8e39b`
- 候选改版：`181ce7cd6e73e1782b56c869873281645b599260`
- 正式状态：用户已确认，固定源码提交 `181ce7cd6e73e1782b56c869873281645b599260`
  已作为 `0.15.6` 正式发布；完整记录见 [`LAUNCHER_RELEASE_0.15.6.md`](LAUNCHER_RELEASE_0.15.6.md)。

## 候选范围

1. 当前服务器详情重新使用上下锚定：标题、说明、状态和分类贴近横幅顶部，主操作与
   三点菜单对齐横幅底边，不再随横幅高度一起居中漂移。
2. 首页主操作由 `156 x 40` 收紧为 `148 x 40`，局部水平内边距改为 `18`；三点菜单
   保持 `40 x 40` 的稳定点击区域。两项操作均不随窗口尺寸拉伸。
3. 只调整首页视觉布局；服务器目录、安装、更新、登录、游戏启动和客户端操作菜单的
   业务绑定与状态机保持不变。

## 候选门槛

- [x] XAML 解析和启动器契约测试 `29/29` 通过。
- [x] 启动器测试 `225/225`、完整解决方案测试 `710/710` 和 Release 构建通过，
  构建警告与错误均为 `0`。
- [x] `1590 x 960`、`1673 x 960`、`2250 x 1290` 与 `2349 x 1529` 真实 WPF 截图中，
  标题和操作分别稳定对齐横幅上下边缘，按钮无裁切、拉伸或异常留白。
- [x] Taste 人工审查、Impeccable 布局扫描和 `git diff --check` 通过。
- [x] 用户已确认候选视觉结果，正式安装包、私有 OSS、生产更新通道和真实登录态更新链
  均已验收。

## 发布边界

- 不修改服务器目录数据、客户端档案、开放状态、公告、活动或游戏启动状态机。
- 不启动、停止或重启 Minecraft、Velocity、服控代理或 Publisher。
- `0.15.5` 的 OSS 对象与 Git 标签保持不可变；正式修复只能使用 `0.15.6` 或更高版本。

## 回滚

生产更新通道已切换到 `0.15.6`。出现分发异常时恢复
`/etc/hechao-launcher-api/environment.launcher-updates.20260809T232544Z.bak` 并只重启
Launcher API，或禁用更新通道；已安装 `0.15.6` 的客户端不会自动降级。`0.15.5` 的
安装包和标签继续保持不可变。
