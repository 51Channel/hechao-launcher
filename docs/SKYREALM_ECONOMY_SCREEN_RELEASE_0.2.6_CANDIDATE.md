# 天域远征工业季第三方屏幕 0.2.6 候选记录

- 记录日期：`2026-08-20`
- 组件：`HechaoEconomyScreen-NeoForge-1.21.1-0.2.6.jar`
- 状态：本地候选，尚未上传 OSS、导入后台或切换任何发布通道
- 网络协议：`2`，服务端命令、权限和经济交易合同未改变

## 本轮修复

针对玩家市场上架页的视觉重叠和信息堆叠，建立集中式布局契约并重新整理四个功能区域：

- 物品槽和价格模块使用稳定的独立矩形，顶部模块与背包模块保留明确间距；
- 价格输入框和“上架”按钮横向排列，控件各自留出边距，不再垂直覆盖；
- 移除价格区额外嵌套的仪表框，避免多层边框糊成一块；
- 玩家背包标题移出模块边界，背包槽位和说明区不越界；
- 服务端状态标题在窄说明栏中使用短状态文案，避免无意义的长文本截断。

## 行为边界

- 继续使用服务端既有槽位：上架 `13`、状态 `4`、确认 `22`、返回 `26`；
- 保留原生物品拖放、Shift 点击、价格校验、提交冷却和隐藏控制槽拦截；
- 上架仍提交既有命令 `hechaoeconomy:ah list <price>`；
- 未新增命令、协议、权限、经济数据写入或服务端逻辑。

## 本地验证

- PowerShell 7 + Java 21 下连续两次执行 `clean test build --no-daemon`；
- Screen JUnit：`85/85`，失败和错误：`0`；
- Impeccable layout detector：无发现；
- `git diff --check`：通过；
- 可复现 JAR：`934,614` 字节；
- SHA-256：`283710D525CD2C53F364555C768EE0E6EA0E27266BBB17061E5140F10DFB68C5`；
- Test、Gray、Production、OSS、API 和 Minecraft 服务端：本轮未操作。

## 发布前门禁

生成客户端档案并发布前，仍需通过启动器增量更新后的真人目视检查：确认上架页四个模块
的间距、价格输入、上架成功/失败退回和无闪屏返回；随后完成双账号购买、下架、待领取、
断线恢复、背包竞争、幂等重试和余额守恒。门禁完成前不得推进 Gray 或 Production；当前
`1.0.25 / Screen 0.2.5` 继续作为回滚目标。
