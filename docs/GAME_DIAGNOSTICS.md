# 游戏退出与脱敏诊断

> 启动器源码版本：`0.11.14`
>
> 状态：本地退出记录、玩家主动生成、再次确认上传、管理员审计和到期删除均已实现

## 1. 目标

诊断能力用于回答三个问题：

1. Minecraft 是正常退出、异常退出，还是启动器没有取得退出码。
2. 当前客户端的最新日志和崩溃报告是否包含可定位的问题。
3. 玩家能否在不暴露账号凭据、世界存档和本机身份的前提下，把必要信息交给管理员。

启动器不会因为诊断功能改变 Minecraft 服务端、Velocity 或任何游戏进程的启动策略。

## 2. 本地文件

退出记录和诊断包位于：

```text
%LocalAppData%\Hechao\Launcher\
|-- game-exits.json
`-- diagnostics\
    `-- Hechao-Diagnostic-<UTC 时间>-<随机 ID>.zip
```

- `game-exits.json` 最多保留 20 条，只记录档案 ID、Minecraft 进程 ID、退出码和起止时间。
- `diagnostics` 最多保留 10 个由玩家主动生成的 ZIP。
- 卸载启动器不会自动删除这些本地支持记录。
- 诊断包不会写入游戏数据根目录，也不会进入客户端发布清单。

## 3. 退出记录

Minecraft 进程成功启动后，启动器订阅进程退出事件：

- 退出码 `0`：记录为正常退出。
- 非零退出码：记录为异常退出，并在启动器仍运行时显示提示。
- 无法取得退出码：记录为状态未知。

“游戏启动后关闭启动器”启用时，启动器退出后无法继续接收进程事件，因此本次退出可能没有记录。玩家下次打开启动器后仍可从设置页主动生成诊断包，因为 Minecraft 自己写入的日志和崩溃报告仍在档案目录中。

退出事件订阅者的异常不会阻止游戏进程对象清理，也不会影响下一次启动。

若进程在一秒内以代码 `1` 退出且没有生成新的 `latest.log`，错误发生在 Minecraft
日志系统初始化之前，应优先捕获 Java 标准错误并检查可执行路径、类路径和主类。
`0.11.9` 针对数据根目录含不可见 Unicode 格式字符的情况，同时为 Java 运行时和
`.minecraft` 建立安全启动别名，避免 Fabric `KnotClient` 因类路径失效而找不到。

## 4. 诊断包内容

玩家在设置页选择“生成诊断包”后，启动器只读取当前服务器对应档案中的：

- `logs\latest.log`；
- `crash-reports` 目录中最新的 `crash-*.txt`；
- 启动器版本、档案 ID、已安装档案版本、Windows/.NET 架构；
- 与当前档案匹配的最近一次退出码和时间。

每个日志文件最多读取末尾 512 KiB。超过限制时丢弃较早内容并写入截断标记，避免异常日志耗尽内存或生成超大压缩包。

固定不会收集：

- `saves` 世界存档；
- 截图、资源包、光影包和模组配置内容；
- 赫朝账号密码；
- Microsoft、Xbox 或 Minecraft 会话缓存；
- 启动器刷新令牌；
- 浏览器历史或其他应用日志。

## 5. 脱敏规则

写入 ZIP 前会替换：

- `Bearer`、Xbox、OAuth、访问令牌和刷新令牌；
- JWT 形态的令牌；
- UUID、长数字账号标识和邮箱；
- IPv4 地址；
- 当前赫朝账号、Minecraft 玩家名和账号标识；
- Windows 用户名、计算机名、用户目录；
- 当前赫朝游戏数据根目录。

固定 ZIP 路径由启动器生成，不采用日志内容作为 ZIP 条目名。日志目录、崩溃目录和源文件若是重解析点会被拒绝。

诊断包仍可能包含玩家在游戏聊天或模组日志中主动输入的其他自由文本。对外发送前，玩家和管理员仍应按最小披露原则处理。

## 6. 玩家操作

1. 打开“设置”。
2. 在“故障诊断”查看最近一次游戏退出状态。
3. 选中发生问题的服务器档案。
4. 点击“生成诊断包”。
5. 启动器完成脱敏后打开本地诊断目录。
6. 需要管理员协助时，再点击“上传给管理员”并阅读确认窗口。

“生成诊断包”和“打开诊断目录”都不会上传文件。只有第 6 步的独立确认动作会申请
10 分钟一次性令牌并上传当前 ZIP。服务端默认保存 14 天，详细边界见
[`DIAGNOSTIC_UPLOAD_OPERATIONS.md`](DIAGNOSTIC_UPLOAD_OPERATIONS.md)。

## 7. 管理员处理

收到诊断包后：

1. 先读取 `diagnostic.json`，确认启动器版本、档案 ID、档案版本和退出码。
2. 查看 `crash-reports`；存在明确崩溃报告时优先处理其异常栈。
3. 再查看 `logs/latest.log` 末尾，定位加载器、模组、Java、显卡驱动或网络错误。
4. 不要求玩家补交 `saves`、账号缓存或完整用户目录。
5. 问题解决后删除管理员侧临时副本。

## 8. 验证基线

`0.9.1` 自动测试覆盖：

- 最新退出记录和 20 条保留上限；
- 日志、崩溃报告和元数据进入固定 ZIP 条目；
- 世界存档不会进入 ZIP；
- 账号名、路径、令牌、UUID、邮箱和 IP 脱敏；
- 超大日志只保留末尾 512 KiB；
- 只选取最新崩溃报告；
- 本地诊断包最多保留 10 个。

正式发布前还要在真实 Minecraft 正常退出和一次受控崩溃场景中各验证一次。

## 9. 上传治理

`0.11.13` 不会自动发送日志。玩家确认上传后，API 同时执行：

- 赫朝账号鉴权和 10 分钟一次性上传授权；
- 文件大小、SHA-256、ZIP 条目、解压大小和档案元数据复验；
- 账号每日次数、每日总大小和有效文件数量配额；
- 完成 MFA 的管理员下载权限与逐次审计；
- 14 天到期标记、文件删除和孤立临时文件清理。

## 10. 真实生产验证

2026-07-27 使用已安装的启动器 `0.11.14` 和真实已登录赫朝账号完成一次玩家确认上传：

- 档案：`base-1.21.11` / `1.0.5`；
- 诊断编号：`1e707520`；
- ZIP：`707` 字节，仅包含 `diagnostic.json` 与 `README.txt`；
- SHA-256：`1C53C309DDA3D1D9A905836E79A041EDCD4DDD03C543E0424119C876AAA6BF92`；
- 本地扫描未命中 Bearer、JWT、IPv4、Windows 用户路径、访问/刷新令牌或世界数据；
- 生产端保存字节数和 SHA-256 与本地完全一致，文件权限为 `0640`，
  所有者与组均为 `hechao-api`；
- 数据库状态为 `uploaded`，到期时间为 2026-08-10；
- `diagnostic.upload.authorized` 与 `diagnostic.upload.completed` 两条审计均存在。

管理员 MFA 已完成真实登记。当前唯一未闭环项是管理员在后台实际下载该编号并核对
`diagnostic.admin.downloaded` 审计；在该记录出现前，完成矩阵仍保持“待生产验收”。
无秘密证据见
[`evidence/ADMIN_MFA_DIAGNOSTIC_UPLOAD_2026-07-27.json`](evidence/ADMIN_MFA_DIAGNOSTIC_UPLOAD_2026-07-27.json)。
