# 天域远征工业季第三方屏幕 0.1.6 Test 发布

- 发布日期：2026-08-18
- 组件：`HechaoEconomyScreen-NeoForge-1.21.1-0.1.6.jar`
- JAR 大小：`33,099` 字节
- JAR SHA-256：
  `E0A740B34D6AE3020C3A117D0054FFD119DD544B0F88B79F4F5AE0890FFC7C67`
- 网络协议：`2`
- 功能源码提交：`71db7d5`
- 客户端档案：`skyrealm-industrial-neoforge-1.21.1 / 1.0.15 / Test=100%`

真实客户端日志确认，旧版按钮点击、发包和服务端响应均正常。菜单在发包后立即关闭，而
余额和出售结果只显示在聊天栏，因此玩家看到的交互像是没有响应。

`0.1.6` 在关闭菜单前先显示动作专属的黄色 ActionBar 提示：

- 六个现有动作都有独立的“正在……”提示；
- 提示只确认客户端已提交动作，不替代服务端最终结果；
- 动作 ID、命令映射、短期会话、权限判断和紧凑双列布局均未改变；
- 网络协议保持 `2`，生产服务端 Screen `0.1.3` 继续兼容。

Gradle `clean test build` 与 `13/13` 测试通过。客户端档案 `1.0.15` 已发布到
`Test=r8 / 100%`；Gray 与 Production 保持未分配。没有重启 API、Minecraft、
Velocity 或服控代理。

玩家必须正常退出旧游戏，再由启动器执行增量更新后验收所有按钮的 ActionBar 提示与
最终服务端结果；完成真人验证前不得推进 Gray 或 Production。
