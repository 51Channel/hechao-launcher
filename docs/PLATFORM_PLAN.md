# 赫朝服务器登录器与分发平台实施文档

> 文档状态：启动器 `0.12.3`、API `0.22.0`、Authorizer `0.4.0` 和
> Lobby Guard `0.1.0` 已生产部署；待真实四级账号与 `2/3/5/20` 人灰度
> 更新日期：2026-07-30
> 范围：玩家登录器、客户端分发、账号与权限、管理员后台、阿里云后端、Minecraft VPS 接入、HTTPS 与迁移
>
> owl9 命名边界：现有目录 ID、Velocity 目标和档案中的 `pvp` 实际代表
> `C:\mc\server` 的恐怖整蛊 Fabric 服。真正 PVP 服是
> `E:\MinecraftServer` 的 Purpur 服，当前未接入启动器且不在本轮验收范围。
> 详见 [`OWL9_DUAL_BACKEND_OPERATIONS.md`](OWL9_DUAL_BACKEND_OPERATIONS.md)。

## 1. 项目目标

把当前启动器升级为整个赫朝 Minecraft 服务器的统一登录入口，而不只是活动服启动器。

玩家使用一个 Windows 客户端完成：

- 登录并绑定正版 Minecraft 身份。
- 查看自己有权限进入的生存服和活动服；大厅只承载内部前置能力，不作为玩家目标。
- 自动安装或更新原版、Paper 兼容客户端、Forge、NeoForge、Fabric 等不同客户端档案。
- 校验客户端文件，缺失时修复，更新失败时安全回滚。
- 从启动器直接进入统一 Velocity 入口，再由代理转入目标服务器。
- 查看服务器状态、在线人数、维护公告和更新进度。
- 在账号区域显示已绑定 Minecraft 身份的真实皮肤头像；网络或皮肤服务异常时使用
  本地默认头像，不影响登录和进服。

管理员通过独立 Web 控制台完成：

- 管理服务器目录、活动、客户端档案、版本和公告。
- 管理玩家等级、白名单和服务器进入权限。
- 发布、暂停、回滚客户端版本。
- 查看下载、登录、权限修改和发布审计记录。
- 查看游戏服务器状态，并通过最小权限本机代理执行带二次确认和审计的优雅启停、
  冲突服切换、快捷设置和 Minecraft 控制台命令。

## 2. 已确认原则

- 游戏 VPS 负责 Velocity 和 Minecraft 服务端，不承担大规模客户端文件分发。
- 阿里云负责 HTTPS API、账号、权限、清单和管理后台。
- 大文件优先放阿里云 OSS，并通过 CDN 或 OSS 下载域名分发。
- `hechao.world` 现有网站必须保留。
- `api.hechao.world` 已用于中转站，不能直接改成启动器 API。
- 启动器 API 使用新子域名，避免影响现有业务。
- 玩家能否进服不能只由客户端判断，客户端只负责展示，Velocity 或后端必须再次强制校验。
- 赫朝启动器是唯一服务器选择与切换入口；游戏内 `/hub`、`/lobby`、NPC 和代理默认回退均不得用于转服。
- Velocity 继续作为唯一公网入口和授权边界；大厅可以保留为首次改写所需的内部引导占位，但玩家不能完成大厅登录，也不能在授权或目标故障时回退到大厅。
- 同一时间只允许一个由启动器管理的 Minecraft 进程；切服必须先安全退出当前进程，再申请新目标授权并启动对应档案。
- 客户端不保存服务器 SSH 密码、阿里云密钥、管理密钥或数据库密码。
- 平台不根据目录或心跳自行启动 Minecraft 服务。只有完成后台 MFA 的管理员明确
  确认后才能排队；启动冲突组目标时可自动优雅关闭同组在线服务端，任一停止失败
  必须取消目标启动。
- 新版本先发布到测试通道，小范围验证后再切换正式通道。
- 官网、启动器及发布材料持续展示非官方产品声明，不暗示获得 Mojang 或 Microsoft 认可。
- 只分发自有模组、配置与资源，不重新分发 Minecraft 本体或经修改的官方游戏文件。

## 3. 当前已完成内容

当前生产为启动器 `0.13.7`、API `0.23.1`、
Authorizer `0.4.0` monitor 和 Lobby Guard `0.1.0`。全局单 Minecraft 进程、
安全退出后再授权、Lobby 玩家目录过滤、基础设施角色、隐藏后继续监控、首次授权
故障关闭和大厅后端独立拒绝均已部署。当前开发分支自动验证为 `.NET 458/458`、
Velocity `26/26`、
Lobby Guard `3/3`、等级代理 `4/4` 和指标代理 `2/2`；Windows
`0.12.0 -> 0.12.1` 的干净安装与双轮卸载、`0.12.1 -> 0.12.2 -> 0.12.3` 覆盖安装通过，私有 OSS 匿名 `403` 与
签名回读 `200` 通过。生产状态的权威入口见
[`COMPLETION_MATRIX.md`](COMPLETION_MATRIX.md)。

旧隔离 Velocity `3.4.0-SNAPSHOT` 与 `3.5.1` 的真实 `/hub` 分别因
`accept_teleportation` 多出 `131` / `16` 字节断开。隔离核心后来使用稳定版
Velocity `4.0.0` build `6` 和独立 Temurin Java `25.0.4+7`，并曾完成五轮跨版本
回大厅实验；该方案已被 2026-07-29 的启动器唯一切服决策取代，只保留历史证据。
生产代理已迁移到 Velocity 4、Java 25 和 Authorizer `0.4.0` monitor。HubCommand、
Via 回程和后端 `/hub` 已备份后退出活动路径；API `0.22.0` 与 Lobby Guard 已部署。
当前剩余工作是完成真实切服与四级账号、切换 `enforce` 和目录强制登录，再执行
`2/3/5/20` 人灰度。大厅任务、等级代理、指标、告警和备份必须始终保留。
schema `2` 灰度工具、匿名授权聚合、enforce 闸门和目录强制登录的备份/自动恢复
工具已于 2026-07-30 完成；它们不会替代真实正版身份和多人负载。执行顺序见
[`GRAY_PILOT_AUTHORIZATION_CUTOVER.md`](GRAY_PILOT_AUTHORIZATION_CUTOVER.md)。

owl9 的恐怖整蛊 Fabric `1.20.1` 后端已经安装官方 FabricProxy-Lite `2.6.0`，
并与 owl5 Velocity 的 modern forwarding 密钥完成摘要核对和 ACL 加固。
恐怖整蛊保持 `online-mode=true`，部署全程没有启动或重启游戏服；当前仍需服主下次
手动开服完成真实统一入口、身份转发和直连拒绝的说明已被后续持久任务验收取代。
2026-07-29 已确认保留大厅作为 LuckPerms 等前置能力的内部承载器，但玩家不能进入。
跨版本回大厅方案不再使用；恐怖整蛊与其他玩家服务器只允许从启动器选择，客户端档案
必须与目标服匹配，Velocity 不再加载只用于大厅回程的 ViaVersion/ViaBackwards。
完整决策和验收标准见
[`LAUNCHER_ONLY_SERVER_SWITCHING.md`](LAUNCHER_ONLY_SERVER_SWITCHING.md)。

2026-07-28 的生产剩余门槛审计确认：22 个社区账号中只有 1 个完成 Minecraft
绑定，生产遥测也只有 1 条启动器启动成功；当前有效后台会话为 0。随后五个游戏目标
均已受控开服并加载指标代理；owl5 三服与 owl9 恐怖整蛊正式世界归档、异机完整
解压、`level.dat` 校验和区域结构恢复均已通过。仍缺真实四级账号与 5 人、20 人
分档灰度。执行顺序见
[`PRODUCTION_ACCEPTANCE_GATES_2026-07-28.md`](PRODUCTION_ACCEPTANCE_GATES_2026-07-28.md)。

- [x] WPF Windows 客户端基础界面。
- [x] 大厅、生存服、活动服目录展示（历史能力；生产 `0.12.3` 已从玩家目录移除大厅）。
- [x] 在线目录、缓存目录和内置应急目录均已移除大厅，同时保留管理员基础设施状态、内部告警和备份。
- [x] 实现启动器唯一切服：单进程、安全退出、退出后重新授权和重启接管；待同档案与跨档案真实三轮切换。
- [x] 将 Velocity 首次引导改成无有效授权必定硬拒绝，并生产部署独立 Lobby Guard。
- [x] 拆分玩家目录角色与 `monitoring_enabled`，使隐藏大厅仍可采集、告警和备份；迁移 `019` 已生产验收。
- [x] 在线、维护、关闭三种状态展示。
- [x] Minecraft 版本和 Vanilla、Paper、Forge、NeoForge、Fabric 类型模型。
- [x] Member、Participant、Collaborator、Administrator 四级权限模型。
- [x] 客户端更新、校验和就绪状态的界面演示。
- [x] 游戏内存和游戏数据目录本地设置。
- [x] 启动时客户端扫描开关、关闭状态提示、重新开启即时检查和进服前强制检查。
- [x] `IServerCatalogClient` HTTPS 实现、上次成功缓存与内置应急目录。
- [x] Windows x64 自包含发布配置、正式安装包和卸载保留游戏数据策略。
- [x] 每个客户端档案独立 `.minecraft`，共享内容寻址对象和受管 Java 运行时。
- [x] 从旧 `%AppData%` 或自定义客户端根目录安全迁移到存储结构版本 `2`。
- [x] 应用多尺寸图标与 Windows EXE 图标资源。
- [x] 独立 PostgreSQL、事务迁移、数据库感知就绪检查和真实目录 API。
- [x] Linux systemd 原子发布、失败回滚和数据库每日备份流程。
- [x] Microsoft 系统浏览器、PKCE、Xbox/XSTS 与 Minecraft 正版验证代码。
- [x] 赫朝 Microsoft 公共客户端注册、个人账户范围与桌面回调配置。
- [x] 短期访问令牌、轮换刷新令牌、撤销与 Windows DPAPI 会话保护。
- [x] LuckPerms 共享数据库只读同步、四级映射和玩家目录授权过滤。
- [x] 生产 API `0.22.0`、客户端兼容保护、基础设施角色、签名清单分发、三通道发布、运行遥测、服务器深度指标、统一告警、敏感 URL 日志脱敏、一次性进服授权、权限同步、状态心跳、赫朝账号、论坛会话联动与账号安全链路部署验收。
- [x] 真实客户端清理、生产签名、内容寻址对象和完整清单校验。
- [x] 受管 Java 21 运行时、Fabric `0.19.2` 进程构建和 Velocity 入口参数。
- [x] 独立的 DPAPI 发布密钥、OSS 只读 API 身份，以及只在对象前缀内执行元数据校验与写入的发布工具。
- [x] Velocity 3.4 / Java 21 异步授权插件、三种运行模式和无自动重启安装脚本。
- [x] owl9 恐怖整蛊 Fabric 后端的 FabricProxy-Lite modern forwarding 静态部署、密钥摘要核对、受限 ACL 和无启动回滚备份。
- [x] 启动器在进程启动前申请一次性授权，API 拒绝时不创建 Minecraft 进程。
- [x] Minecraft 正常/异常退出记录、异常提示和玩家主动生成本地脱敏诊断包。
- [x] 退出所有设备、撤销待使用后台/进服授权和密码确认解除 Minecraft 身份绑定。
- [x] 按 Velocity 目标采集 Minecraft 状态、在线人数和版本；一分钟计划任务不含任何进程控制能力。
- [x] 管理员服务器目录 CRUD、归档/恢复、修订冲突和事务审计。
- [x] 启动器管理员入口、一次性浏览器票据、独立 Cookie 会话、TOTP MFA、恢复码和 CSRF。
- [x] 管理后台服务器目录、只读客户端档案和审计记录界面。
- [x] 管理后台服务器公告、一次性开放/关闭排期及玩家目录状态合并。
- [x] 管理后台玩家搜索、最终访问预览和带有效期的单服允许/拒绝规则。
- [x] 管理后台账号停用/恢复、单设备与全部会话撤销、Minecraft UUID 封禁及事务审计。
- [x] 管理后台客户端档案创建、签名清单导入、Test/Gray/Production、稳定分桶、暂停与自动回滚。

更新、修复和“进入服务器”已经接入真实实现。基础 Fabric、Vanilla、Forge、NeoForge、
恐怖整蛊 Fabric 与 DollNight 六份生产签名档案、私有 OSS、API `0.22.0` 与五目标实时
心跳均已在线；Survival1 和 DollNight 已绑定各自档案，Forge 保持未绑定直到真实服务
器建立。发布分支 `.NET 392/392`、Velocity `26/26`、Lobby Guard `3/3`、等级代理
`4/4` 与指标代理 `2/2` 通过。启动器单进程切服和基础设施大厅隔离已经生产部署；
Activity 在含 U+200C 的既有数据根目录下已由 `0.12.3` 改用物理 `native-runs`
目录，并完成五项原生属性规范化和安装版真实进服；四级真实
账号、成功重连、Velocity `enforce`、目录强制开关和多人灰度仍未完成。

## 4. 推荐总体架构

```text
玩家启动器
  | HTTPS 登录、目录、权限、清单
  v
阿里云 API / 管理后台 / 数据库
  |
  +---- OSS + CDN：JRE、模组、配置、资源包、启动器更新
  |
  +---- 状态采集：只读取游戏 VPS 状态

玩家 Minecraft 客户端
  | Minecraft 协议
  v
游戏 VPS 的 Velocity 统一入口
  |
  +---- 大厅
  +---- survival2 生存服
  +---- NeoForge / Forge / Fabric 活动服
  +---- DollNight 等插件活动服
```

### 4.1 游戏 VPS

职责：

- 运行 Velocity、大厅、生存服和活动服。
- 向状态采集服务提供在线状态、人数、版本和维护状态。
- 在 Velocity 或后端服执行最终进服权限判断。
- 保存世界、插件、模组和运行日志。

不承担：

- 玩家客户端下载。
- 用户密码登录。
- 公网管理后台。
- 将 SSH 能力直接暴露给启动器。

### 4.2 阿里云

职责：

- 登录、令牌、账号绑定和权限 API。
- 服务器目录和客户端档案 API。
- 管理员 Web 控制台。
- 发布清单、审计日志和状态缓存。
- OSS/CDN 文件分发。
- HTTPS 证书终止和反向代理。

阿里云 ECS 适合运行轻量 API，客户端下载流量应尽量交给 OSS/CDN，避免带宽和系统盘成为瓶颈。

## 5. 域名规划

| 域名 | 用途 | 处理原则 |
| --- | --- | --- |
| `hechao.world` | 现有网站 | 保持现状，不在首轮迁移中改业务 |
| `api.hechao.world` | 现有中转站 API | 保持接口和监听，迁移时只做兼容代理 |
| `launcher-api.hechao.world` | 启动器与管理后台 API | 已新增，A 记录指向阿里云主机并通过 HTTPS 代理到回环 API |
| `download.hechao.world` | OSS/CDN 客户端文件 | 新增，CNAME 到 OSS/CDN |
| `admin.hechao.world` | 管理员控制台 | HTTPS 与 `AdminWeb` 已启用；Host 锁定生效，真实管理员 MFA 已登记，逐页生产验收进行中 |

可以使用一张包含上述子域名的 SAN 证书，或使用 DNS 验证签发通配符证书。证书私钥只能保存在阿里云反向代理上。

## 6. HTTPS 与迁移策略

配置 HTTPS 本身不会必然导致 `api.hechao.world` 或 `hechao.world` 无法访问。真正的风险来自端口冲突、错误反向代理、DNS 提前切换和原服务监听地址被直接修改。

### 6.1 迁移前盘点

- [x] 记录阿里云实例的 CPU、内存、系统盘、数据盘、公网带宽和操作系统（带宽计费规格仍需控制台复核）。
- [x] 记录当前 `80`、`443` 及业务端口的监听进程。
- [x] 导出当前 Nginx、Caddy、IIS 或应用自身的监听配置。
- [x] 记录 `hechao.world` 和 `api.hechao.world` 的 DNS 解析、TTL 和证书状态。
- [x] 逐项记录现有网站和中转 API 的健康检查 URL。
- [ ] 创建系统快照，并单独备份配置、数据库和上传文件。
- [ ] 确认回滚负责人、回滚命令和最长可接受中断时间。

实时盘点见 [`ASSET_INVENTORY.md`](ASSET_INVENTORY.md)。当前已有一次可校验的迁移归档，但尚未复核云快照、每日备份和恢复演练，因此最后两项保持未完成。

### 6.2 安全上线顺序

1. 先部署新的启动器 API，监听 `127.0.0.1` 的独立高位端口。
2. 不修改现有网站和中转 API，先用本机请求验证新 API。
3. 使用当前已有的反向代理增加新子域名，不能再安装一个争抢 `80/443` 的代理。
4. 若当前业务直接占用 `80`，先用 DNS 验证签发证书，再安排一次有回滚方案的监听切换。
5. 新增 `launcher-api.hechao.world`，只把新域名指向新 API。
6. 新增 `download.hechao.world`，指向 OSS/CDN。
7. 分别验证旧网站、旧中转 API、新 API 和下载域名。
8. 观察错误率和延迟后，再考虑是否给现有域名补齐 HTTPS。

截至 2026-07-28，步骤 1 至 7 已完成：API `0.20.2` 仅监听 `127.0.0.1:8090`，启动器 API、管理后台、私有 OSS 和旧网站/中转 API 均保持 HTTPS 正常。数据库、真实目录、认证、账号安全、论坛联动、受控等级、诊断、遥测、统一告警、日志脱敏、排期、单服规则、六份档案、三通道发布、Velocity 授权与客户端兼容保护、五目标心跳和深度指标均已接入；管理员 MFA 已登记，步骤 8 的真实多人持续观察仍在进行。

### 6.3 系统迁移原则

- [ ] 不在原机直接重装系统。
- [ ] 如需换系统，创建新实例或新系统盘，完成平行部署和验证后再切 DNS。
- [ ] DNS 切换前至少提前一天降低 TTL。
- [ ] 新旧环境至少并行保留一个观察周期。
- [ ] 数据库切换前停止写入或使用可验证的增量同步。
- [ ] 旧环境在确认无误前不删除。

## 7. 登录和身份体系

### 7.1 玩家登录

- [x] 首次正版身份交换时建立稳定的赫朝内部用户 ID。
- [x] 使用 Microsoft OAuth 完成 Minecraft 正版身份验证，不采集 Microsoft 密码。代码、Client ID 与 Minecraft API 许可均已完成；真实四级账号生产验收作为独立上线门槛保留。
- [x] 将内部账号与 Minecraft UUID 绑定，并由 Minecraft Services 复核权益与档案。
- [x] 访问令牌使用短有效期，刷新令牌可撤销并在刷新时轮换。
- [x] Windows 客户端令牌使用 DPAPI 保护，MSAL 缓存使用 Windows 安全存储。
- [x] 提供退出所有设备和解除绑定能力；启动器 `0.10.0` 候选已完成，API `0.10.1` 已通过生产隔离账号验收。

### 7.2 等级与服务器权限

等级可以决定默认权限，但数据库应保存明确授权，避免等级规则变化后难以处理例外。

当前 LuckPerms 等级由社区管理员依据参与和运营职责分配，不通过付费、站外购买或外部产品获得；任何等级都不能绕过 Minecraft 正版权益校验。

```text
用户 -> 角色 -> 权限
用户 -> 服务器例外授权或拒绝
服务器 -> 最低等级 + 开放时间 + 白名单策略
```

- [x] 定义现有称号到四级 `AccessTier` 的映射。
- [x] 确定 LuckPerms 共享 MariaDB 为称号权威来源。
- [x] 后端只向已认证玩家返回其等级和单服规则允许的服务器。
- [x] Velocity 插件在连接时异步校验 UUID 和服务器授权；已安装为 `monitor`，待真实账号灰度后切换 `enforce`。
- [x] 权限同步缓存时间为 5 分钟；登录后目录请求读取最新已同步结果。
- [x] 所有管理员授权、拒绝和等级修改均在同一事务写入审计日志。

客户端自动识别只用于改善界面，不能作为唯一防线。修改本地文件或伪造 API 响应不能获得进服权限。

## 8. 服务器目录与客户端档案

服务器目录至少需要以下字段：

- 服务器 ID、显示名、图标和排序。
- 状态、在线人数、最大人数和维护公告。
- Minecraft 版本、加载器类型和加载器版本。
- Velocity 目标服务器 ID。
- 最低等级、白名单和开放时间。
- 对应客户端档案 ID。
- 是否在玩家端显示、是否允许进入。

客户端档案至少需要以下字段：

- 档案 ID、显示名、版本和发布通道。
- Minecraft、Java、加载器版本。
- 主清单 URL、清单签名和发布时间。
- JVM 参数、推荐内存和最大内存。
- 启动前检查规则和目标 Velocity 地址。
- 是否强制更新、最低兼容版本和回滚版本。

同一个基础文件只存一份。大厅和 `survival2` 可共用基础档案；活动服通过继承基础档案叠加自己的模组、配置和资源包。

## 9. 客户端下载与安装

### 9.1 发布格式

每个客户端版本发布一份签名 JSON 清单。每个文件记录：

```json
{
  "path": "mods/example.jar",
  "size": 3238514,
  "sha256": "...",
  "url": "https://download.hechao.world/objects/...",
  "required": true
}
```

清单还应包含档案版本、Minecraft 版本、Java 版本、加载器版本、删除列表和数字签名。

### 9.2 启动器更新流程

1. 获取当前用户可用服务器和客户端档案。
2. 下载并验证签名清单。
3. 对比本地文件的路径、大小和 SHA-256。
4. 将缺失或变化的文件下载到临时目录。
5. 支持 HTTP Range 断点续传、超时、重试和限并发。
6. 下载结束后逐文件校验 SHA-256。
7. 使用目录重命名完成原子切换。
8. 保留上一个完整版本用于回滚。
9. 启动成功后再清理过旧缓存。

启动器本体使用独立自更新链路：登录后从 API 获取版本、最低支持版本、安装包长度、
SHA-256 和短时私有 OSS 地址；后台断点下载并复验后，由当前单文件 EXE 的临时副本
等待主程序退出、静默覆盖安装并重新拉起。任一步骤失败都保留当前安装版本。发布和
回滚流程见
[`LAUNCHER_SELF_UPDATE_OPERATIONS.md`](LAUNCHER_SELF_UPDATE_OPERATIONS.md)。
玩家安装首个支持自更新的正式版本后，后续常规版本由启动器内完成，不需要每次重新
下载安装包；只有安装损坏、自更新器被安全软件持续拦截或跨代不兼容时才使用完整
安装包修复。

### 9.3 必须处理的情况

- [x] 首次安装和全文件级增量更新。
- [x] 用户手动删改模组后的修复。
- [x] 磁盘空间不足。
- [x] 下载中断、代理错误和文件损坏。
- [x] OSS/CDN 暂时不可用时重试并保留当前可用客户端，不提前切换目录。
- [x] 服务端更新后客户端仍是旧版本。
- [x] 不同活动服使用同名但不同版本模组。
- [x] 强制删除已下线模组和旧配置。
- [x] 杀毒软件占用文件导致无法替换并恢复当前版本。
- [x] 客户端版本回滚。

第一版使用全文件级增量更新即可，不必先做二进制差分。文件去重、压缩和 CDN 缓存已经能解决大部分带宽问题。

## 10. Java、加载器与游戏启动

- [x] 为 Minecraft `1.21.11` 指定受控 Java 21 运行时。
- [x] Java 运行时安装到启动器受管目录，不依赖玩家本机环境。
- [x] 将版本 JSON、依赖库、资源文件、Fabric 客户端和 NeoForge 活动客户端纳入签名档案。
- [x] 实现 Vanilla 安装与启动；`vanilla-1.21.11` 已完成全量安装、逐文件复验、进程构建和生产发布，测试未启动游戏。
- [x] 实现 Fabric 安装与启动。
- [x] 实现 Forge 安装与启动；Forge `47.4.0` 已完成全量安装、逐文件复验、进程构建和生产发布，等待真实服务器后再绑定。
- [x] 实现 NeoForge 安装与启动；`21.11.42` 活动档案已完成全量安装、逐文件复验、进程构建，以及 `0.12.3` 物理 `native-runs`、五项原生属性规范化、Windows 特殊路径和安装版真实进服验收。
- [x] 正确生成 classpath、native 路径、JVM 参数和游戏参数。
- [x] 使用 OAuth 会话启动，不把访问令牌写入日志。
- [x] 启动目标统一指向 Velocity 公网入口 `mc.hehe11.fun`。
- [x] 进入活动服前确认服务端在线、权限有效、客户端版本匹配；UI 只允许在线目标启动，启动前重新申请一次性 Velocity 授权，档案状态与签名清单单独校验。
- [x] Minecraft 退出后记录退出码，并允许玩家主动生成本地脱敏诊断包。
- [x] 建立需要玩家明确确认、带鉴权、限额和 14 天保存期限的诊断包上传链路；API `0.13.0` 已完成生产错误路径与清理验收，真实管理员 MFA 下载、文件哈希和审计已完成。

## 11. 管理员 Web 控制台

### 11.1 服务器目录 CRUD

- [x] 新增、编辑、归档和恢复服务器。
- [x] 设置显示名、短名、图标、排序和状态。
- [x] 设置维护公告与开放时间。
- [x] 绑定客户端档案和 Velocity 目标。
- [x] 设置最低等级。
- [x] 设置单服白名单、拒绝规则和有效期。
- [x] 预览某个玩家实际可见和可进入的服务器。

### 11.2 客户端版本管理

- [x] 创建客户端档案。
- [x] 上传构建产物并导入由离线发布器签名的清单。
- [x] 服务端重新计算并验证签名信封 SHA-256，不信任浏览器提交值。
- [x] 发布到测试、灰度和正式通道。
- [x] 查看下载量、失败率和客户端版本分布；启动器 `0.11.16` 为私有 OSS 灰度候选，API `0.20.2` 已通过生产回归；管理员 MFA 已登记，恐怖整蛊隔离回程首条真实会话已通过，仍待重连和多人事件样本。
- [x] 一键暂停问题版本并把通道回滚到上一份未暂停发布。

### 11.3 用户与权限

- [x] 按昵称、UUID、内部 ID 搜索玩家。
- [x] 通过固定四级映射和 LuckPerms 代理修改全局等级；基础设施大厅上的代理已加载并读回真实 `owner` 映射。
- [ ] 保留大厅等级代理，并完成四级真实写入、幂等重试、队列恢复和大厅玩家隔离后的回归验收。
- [x] 停用/恢复账号、撤销设备和平台会话、封禁/解除 Minecraft UUID，并记录原因与审计。
- [x] 通过 PostgreSQL outbox 和论坛幂等回执联动撤销已经签发的 Cookie 会话。
- [x] MFA 机制已部署；真实管理员首次 TOTP 和恢复码登记已完成，生产凭据、恢复码哈希、有效会话和审计均已核对。
- [x] 归档与恢复要求二次确认；权限和发布等后续高风险操作仍未实现。

### 11.4 运行状态

- [x] 展示 Velocity 和各后端服是否在线、人数、内存、CPU、磁盘和启动时间；五个目标均已加载代理并取得 TPS/MSPT/GC。
- [x] 展示最后一次心跳和固定问题代码摘要，不上传异常正文。
- [x] 实现独立最小权限服控代理、结构化启停队列、冲突组串行切换、命令白名单、
  二次确认、完整审计和失败关闭。
- [x] 实现 `server.properties` 白名单快捷设置和受限 Minecraft 控制台；不开放
  PowerShell、CMD、SSH、任意文件或任意进程终止。
- [x] 完成两台 VPS 的真实任务、目录与冲突组盘点并启用 `ServerControl`；当前
  9 个目标、2 个在线代理、4 个运行中实例持续入库，部署前后游戏 PID 未变化。见
  [`SERVER_CONTROL_AGENT_OPERATIONS.md`](SERVER_CONTROL_AGENT_OPERATIONS.md)。

目录中的“关闭”只表示玩家不可进入，不等于自动结束 Java 进程。

截至 2026-07-31，控制台身份、目录、账号、发布、运行数据、状态、告警、诊断、
审计和服控模块已进入生产。服控面板、API 结构化队列和 Windows 本机代理已启用；
离线端口探测回归已修复，真实启停动作仍限定为专用无玩家目标。真实管理员 MFA 与
诊断下载已验收；大厅等级代理已加载并
读回真实 `owner` 映射，但四级账号游戏内往返写入仍需逐项验收。

## 12. API 初版范围

玩家端：

```text
POST /v1/auth/minecraft/exchange
POST /v1/auth/refresh
POST /v1/auth/logout
GET  /v1/me
GET  /v1/catalog
GET  /v1/profiles/{id}/manifest
POST /v1/launch-tickets
POST /v1/diagnostics
```

管理端：

```text
GET/POST/PATCH /v1/admin/servers
GET/POST/PATCH /v1/admin/profiles
POST           /v1/admin/releases
POST           /v1/admin/releases/{id}/promote
POST           /v1/admin/releases/{id}/rollback
GET/PATCH      /v1/admin/users/{id}/access
GET            /v1/admin/audit-logs
```

内部接口：

```text
POST /v1/internal/server-heartbeats
POST /v1/internal/velocity/authorize
POST /v1/internal/luckperms/snapshot
```

所有写接口应支持权限检查、幂等、参数验证和审计。

## 13. 数据与安全

- [x] 生产数据库使用独立账号和最小权限。
- [x] 数据库秘密和内部同步令牌只放受限环境文件或 DPAPI；API 只保存同步令牌 SHA-256。
- [x] 管理 API 与玩家 API 使用不同认证方案；启动器 Bearer 只能创建短时票据，管理写入要求独立 MFA 会话与 CSRF。
- [x] API 已启用全局、认证、内部同步、Velocity、心跳和内部告警分级速率限制；API `0.20.2` 保留统一告警、敏感 URL 日志脱敏和客户端兼容保护。
- [x] OSS 默认私有，使用启动器 API 鉴权和短时 V4 签名 URL。
- [x] 清单使用离线发布密钥签名，启动器内只包含公钥。
- [x] API、数据库和对象文件建立独立恢复链并定期演练；启动器数据库、论坛与 Sub2API 的真实 OSS 与异地主机恢复已完成，六个活动签名档案的 `8,944` 个内容寻址对象也已写入 OSS 外的独立系统盘副本并通过全量哈希与隔离恢复。当前不是跨云厂商或跨地域副本，边界见 [`DISTRIBUTION_OBJECT_RECOVERY.md`](DISTRIBUTION_OBJECT_RECOVERY.md)。
- [x] 当前 API journal 与 Nginx 访问日志不记录 OAuth/OSS 签名 URL、查询参数、Referer、SSH 密码或数据库密码；历史轮转日志保留且权限受限，不破坏审计链。
- [x] 已实现目录变更和管理登录/MFA操作审计；后续模块接入时继续沿用事务审计。
- [x] 游戏 VPS 不暴露状态接口；采集器只做本机探测并主动通过 HTTPS 向阿里云 API 上报，API 使用独立心跳令牌认证。

## 14. 容量评估

现阶段不能仅凭“VPS”和“阿里云”判断配置是否足够，必须先记录实际规格和峰值数据。

### 14.1 需要采集

- [x] 游戏 VPS 的 CPU 型号、核心数、内存、虚拟 SSD/SAS 介质和两卷剩余空间已记录。
- [x] 四个 Java 进程的 `Xms/Xmx`、当前/峰值工作集、累计 CPU 与读写传输量已保存为机器可读快照。
- [x] Velocity、大厅、Survival2 与 Activity 同时运行的空载进程、内存、CPU、磁盘和 Tick 指标已持续采集；NeoForge 零玩家暂停由采集器 `0.2.1` 显式识别。Survival1 当前关闭，全部玩家服同时运行仍由真实多人灰度覆盖。
- [ ] 20 至 30 人活动时的 TPS、MSPT、GC 暂停和网络出站。
- [x] 阿里云分发主机的 2 vCPU、可见内存、系统盘余量与控制台 200 Mbps 峰值已核对；峰值不作为承诺带宽。
- [x] 当前中转 API 已建立隐私日志后的真实基线：32 分钟 `269` 请求、全部 `200`、峰值约 `0.267 QPS`、响应体峰值约 `0.308 Mbps`；流式 `/responses` 时长单独解释。
- [x] 六个正式档案完整大小已核对为 `549,101,696` 至 `885,821,291` 字节；当前 `22` 个启用账号，按每日 `30` 人、正常 `20` 并发和容量目标 `30` 并发规划，30 人最坏冷安装在 30 分钟窗口需要约 `135.83 Mbps`（含 15% 余量）。

### 14.2 判断标准

- 游戏开始后 MSPT 持续低于 50 ms，避免 TPS 长时间低于 20。
- Minecraft 满载时系统仍保留内存和 CPU 余量，不能依赖交换分区维持。
- API 峰值 CPU、内存、连接数和数据库连接池有至少约 30% 余量。
- 客户端下载不会经过游戏 VPS。
- 阿里云 API 带宽不承载完整模组包，文件由 OSS/CDN 直接下发。

## 15. 监控、备份与发布

- [x] API 健康检查、延迟、5xx、登录失败和下载失败监控。
- [x] 游戏服在线状态、端口、进程内存、CPU、启动时间、磁盘与五目标 TPS/MSPT/GC 监控已上线；采集器 `0.2.1` 不再把 Activity 零玩家暂停误报为指标过期，有人在线时仍严格拒绝旧指标。20 至 30 人负载仍待活动窗口。
- [x] 私有 OSS 下载入口匿名 `403` 基线、对象授权 5xx 和客户端实际下载失败率监控；当前未启用 CDN，因此没有 CDN 命中率指标。
- [x] 证书链验证和到期 30/7 天分级告警。
- [x] 启动器数据库每日本机备份、异地加密上传、立即下载复验、定时任务、告警恢复和异地主机隔离恢复。
- [x] 论坛与 Sub2API 在线一致性备份、systemd 沙箱、RAM v5、加密 OSS 往返、owl5 解密、77 表隔离恢复、每日定时器和失败/恢复邮件均已验收。
- [x] Minecraft 世界已按服接入 VSS 短冻结、后台压缩、串行、校验、原子完成的备份引擎；Lobby、Survival1、Survival2 正式归档、异机完整解压、`level.dat` 校验和区域抽样恢复均已通过，owl9 恐怖整蛊也已完成正式归档、逐文件哈希和区域全量恢复检查。
- [x] 客户端版本、清单和对象文件不可覆盖，使用不可变版本号和内容寻址对象。
- [x] 六个活动签名档案的完整对象恢复集已在独立系统盘保存，`8,950` 个清单/对象文件通过本地、远端和隔离恢复三重校验；每次档案发布后必须重建并原子切换恢复集。
- [x] 当前全部活动发布均在机器可读台账中保留构建来源、SHA-256、注释标签发布人和回滚目标；校验脚本会拒绝缺项、轻量标签、错误标签落点和仓库外证据路径。

## 16. 分阶段实施顺序

### 阶段 0：资产盘点与保护现有业务

- [x] 两台 Minecraft VPS 与阿里云实例均已完成无密码资产清单；`owl9` 已恢复公钥登录并记录 Windows、4 逻辑核、8 GiB、两卷磁盘和双服务端状态，代表恐怖整蛊历史目标 `pvp` 的独立只出站采集器也已上线。
- [x] 记录现有端口、进程、域名、DNS、反向代理和证书。
- [x] 完成应用配置、数据库、论坛、Sub2API、游戏世界和生产切换回滚演练；云主机系统快照仍由 6.1 的独立未完成项跟踪。
- [x] 确认 `hechao.world` 与 `api.hechao.world` 健康检查基线。

完成标准：知道每个服务在哪里运行、如何验证、如何回滚，且文档不含明文秘密。

### 阶段 1：HTTPS 与后端骨架

- [x] 新建 `launcher-api.hechao.world` 和 `admin.hechao.world`。
- [x] 部署只监听回环地址的 API 骨架和健康检查。
- [x] 部署独立数据库及最小权限数据库账号。
- [x] 为新域名部署 Nginx 独立 server block 和 HTTPS。
- [x] 建立并部署管理员登录、MFA、角色和审计日志；首次真实 MFA 登记待完成。
- [x] 保持现有网站和中转 API 正常。

完成标准：新 API 可通过 HTTPS 访问，旧域名检查结果与迁移前一致。

### 阶段 2：目录、权限与状态

- [x] 用真实 API 替换 `DemoServerCatalogClient`，并保留缓存与内置目录降级。
- [x] 完成账号绑定、等级映射、目录服务器授权和 Velocity 最终拦截实现；正式登录激活与 `enforce` 灰度待完成。
- [x] 接入只读游戏服心跳；按 Velocity 目标合并替换服，过期或离线心跳不会触发服务端启动。
- [x] 完成并部署服务器目录 CRUD、维护状态、Web 控制台与 MFA 机制；真实管理员写入灰度待完成。

完成标准：不同等级账号只能看到和进入被授权服务器，伪造客户端状态无法绕过后端。

### 阶段 3：客户端分发

- [x] 建立私有 OSS 和 `download.hechao.world`，完成 CNAME 与 HTTPS。
- [x] 完成清单生成、签名、上传、发布和回滚；独立发布身份与 `4,900` 个不可变对象已上线。
- [x] 启动器完成断点续传、校验、修复和原子切换，生产公钥已内嵌。
- [x] 建立基础、Vanilla、Forge、NeoForge 活动、恐怖整蛊 Fabric 与 DollNight 六份生产签名档案。

完成标准：干净电脑可以安装，断网后可续传，篡改文件会被修复，坏版本可以回滚。

### 阶段 4：真正启动和进服

- [x] 完成 Microsoft OAuth 和 Minecraft 会话实现，Minecraft API 许可已经通过；仍需完成真实账号验收。
- [x] 完成基础 Fabric、Vanilla、Forge、活动 NeoForge、恐怖整蛊 Fabric 和 DollNight 档案；按档案使用 Java 21 或 Java 17、独立目录和统一 Velocity 入口。
- [x] 完成 Velocity 入口连接、短时启动授权、首次目标定向和客户端兼容保护；生产 `0.4.0` 已将所有首次连接失败改为硬拒绝并保持 `monitor`。
- [x] 生产完成基础设施大厅映射收口：大厅不可授权、首次引导无授权硬拒绝、后端独立拒绝玩家；待真实四级账号灰度和 Velocity `enforce`。
- [x] 处理正常/异常退出和本地脱敏诊断。
- [x] 提供玩家主动确认的诊断上传；真实 `0.11.14` 已上传编号 `1e707520`，隐私扫描、上传端/生产端/管理员下载哈希、一次性令牌、上传与下载审计和 14 天到期均已核对。

完成标准：玩家从登录器点击一次即可进入被授权目标服，未授权账号被服务端拒绝。

### 阶段 5：灰度与正式上线

- [ ] 管理员和内部成员先测试。
- [ ] 选择 5 人、20 人两档灰度。
- [x] 断网、损坏文件、服务器维护、权限变化和版本回滚已完成可重复自动化故障演练；完整 `.NET 355/355`、五类定向集合 `45/45` 与三组服务端代理 `17/17` 通过，真实玩家表现继续在分档灰度验收。
- [x] 每次生产切换均回归旧网站与中转 API；本轮游戏 Java PID、启动时间和监听保持不变。
- [x] 编写玩家安装说明和管理员发布手册；见 [`PLAYER_INSTALLATION_GUIDE.md`](PLAYER_INSTALLATION_GUIDE.md) 与 [`ADMIN_RELEASE_RUNBOOK.md`](ADMIN_RELEASE_RUNBOOK.md)。

完成标准：连续运行一个观察周期无阻断级故障，并完成一次真实回滚演练。

## 17. 近期优先任务

按依赖关系，下一步只做以下事项：

1. 完成两台服务器与阿里云的规格、端口、域名和服务盘点（已完成；`owl9` 的公钥管理基线、双服务端边界、恐怖整蛊独立只出站状态采集与 modern forwarding 静态兼容已上线）。
2. 确认现有 `hechao.world`、`api.hechao.world` 的监听方式和反向代理（已完成）。
3. 确定新域名并部署不影响旧业务的 HTTPS 测试 API（已完成）。
4. Minecraft Java API 访问许可已经通过；完成真实普通、VIP、管理员和服主账号验收。
5. 定义数据库表和 API 契约，用真实目录 API 替换演示数据（已完成）。
6. Velocity `0.4.0`、API `0.22.0`、启动器 `0.12.3` 与 Lobby Guard `0.1.0` 已按可回滚顺序部署，HubCommand、Via 回程和后端游戏内转服已退出活动路径；下一步用真实四级账号验证直接路由、切服、旁路拒绝和断线恢复。
7. 创建独立发布 RAM 身份，将 AccessKey 保存为 DPAPI 密文；v3 仅允许 `hechaoworld/objects/*` 与 `hechaoworld/releases/launcher/*` 的 `GetObject/PutObject`（已完成）。
8. 上传已校验的基础档案对象，部署 API `0.5.0`，再原子发布清单和目录记录（已完成）。
9. 使用普通、VIP、管理员和服主正版账号完成下载、安装、Java 17/21、Fabric/NeoForge 启动和 Velocity 进服验收。
10. 部署按 Velocity 目标的只读心跳、API `0.6.0` 和一分钟计划任务（已完成，不控制 Minecraft 进程）。
11. API `0.22.0`、客户端兼容保护、基础设施大厅隔离、启动器唯一切服、五目标指标、数据库/平台异地恢复、owl5 三服和 owl9 恐怖整蛊世界正式备份/隔离恢复均已生产部署或验收；继续完成真实四级账号和多人灰度。跨版本回大厅已取消。
12. 发布包含启动器自更新和真实皮肤头像的下一版候选；服控先以关闭状态部署 API，
    完成两台 VPS 只读盘点和专用目标验收后再单独启用。

## 18. 待确认事项

- [x] 两台 VPS 与阿里云实例的实际规格和系统版本已记录；`owl9` 于 2026-07-28 复核为 Windows Server 2022、4 逻辑核、8 GiB。
- [x] `hechao.world` 和 `api.hechao.world` 当前由哪个进程监听 `80/443`。
- [x] 现有玩家称号和权限数据来自共享 MariaDB 中的 LuckPerms。
- [x] 强制 Microsoft 正版登录；正式开关在 API 许可、真实账号验收和 Velocity `enforce` 完成后启用。
- [x] Velocity 公网入口保持 `mc.hehe11.fun`；Minecraft SRV 记录指向外部端口 `15156`，转发到内部 `25577`，2026-07-26 重启后已从公网验证 TCP 可达。
- [x] 基础客户端完整包逻辑大小为 `874,147,856` 字节；常规更新差量待首次版本迭代统计。
- [x] 阿里云 OSS、HTTPS 证书和独立启动器数据库均已开通；当前没有启用 CDN。
- [x] 管理后台运行控制采用独立最小权限代理；玩家端无权使用，生产必须默认关闭并
  通过专用目标验收后启用。

## 19. 明确不做

- 不把 SSH 密码或云密钥打包进启动器。
- 不让客户端直接连接数据库。
- 不依赖客户端单方面决定进服权限。
- 不直接覆盖客户端当前可用版本。
- 不让客户端下载占用游戏 VPS 带宽。
- 不为配置 HTTPS 而直接重装现有阿里云系统。
- 不在未盘点端口和进程前修改 `80/443` 监听。
- 不在平台建设和测试过程中擅自启动 Minecraft 服务端。
