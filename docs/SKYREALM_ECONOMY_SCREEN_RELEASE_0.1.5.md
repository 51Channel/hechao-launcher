# 天域远征工业季第三方屏幕 0.1.5 Test 发布

- 发布日期：2026-08-18
- 组件：`HechaoEconomyScreen-NeoForge-1.21.1-0.1.5.jar`
- JAR 大小：`32,811` 字节
- JAR SHA-256：
  `9E253BDDB82A5FDCB0B044D2C41303C86C4877308E785F32D175E30FCC56561B`
- 网络协议：`2`
- 功能源码提交：`a6137c7`
- 客户端档案：`skyrealm-industrial-neoforge-1.21.1 / 1.0.14 / Test=100%`

该版本按玩家提供的 Donut SMP 参考图重构业务屏幕。旧版宽深色面板、黄色竖线、
副标题和按钮说明已移除，改为直接覆盖在游戏画面上的原版 Minecraft 快捷菜单：

- 标题与 `20 px` 授权提示按钮居中；
- 普通窗口采用双列布局，总宽约为窗口宽度 `63%`，最大 `406 px`；
- 原版按钮高度为 `20 px`，列间距 `6 px`、行间距 `5 px`；
- 常见 `512 x 270` GUI 下六个入口形成 `2 x 3` 网格，每个按钮宽 `158 px`；
- 窄窗口切换单列，矮窗口保留分页和滚轮翻页。

六个现有入口、动作 ID、短期会话、动作白名单和服务端权限检查均未改变。Gradle
`clean test build` 与 `12/12` 测试通过，Impeccable 静态检测为 `0` 项。网络协议保持
`2`，生产服务端 Screen `0.1.3` 可继续兼容，因此没有重启 API、Minecraft、Velocity
或服控代理。

客户端档案 `1.0.14` 已发布到 `Test=r7 / 100%`；Gray 与 Production 保持未分配。
发布时本机仍有 Java 进程运行，未读取其命令行，也未热替换客户端文件。玩家必须正常
退出旧游戏，再由启动器执行增量更新后进行真人视觉验收；完成前不得推进 Gray 或
Production。
