# 天域远征工业季第三方屏幕 0.2.4 候选记录

- 记录日期：`2026-08-19`
- 组件：`HechaoEconomyScreen-NeoForge-1.21.1-0.2.4.jar`
- 状态：本地候选，未上传 OSS，未导入客户端档案，未切换 Test、Gray 或 Production
- 网络协议：`2`，服务端协议和经济交易合同未改变

## 本轮改版

玩家市场的上架页改为明确的功能模块布局：

- 顶部只承载页面标题和返回操作，缩短标题带，给正文留出稳定起点；
- 左侧“物品槽”独立承载可交互上架槽和物品摘要；
- 右侧“定价与操作”独立承载价格输入框和确认上架按钮；
- 左下“玩家背包”独立框住原生背包槽位，继续保留拖放和 Shift 点击；
- 右下“上架说明”独立显示当前服务端状态、最低价格、手续费和操作顺序；
- 移除跨区域长文案，避免状态、价格和背包槽位互相覆盖。

## 行为边界

- 继续使用服务端既有槽位：上架 `13`、状态 `4`、确认 `22`、返回 `26`；
- 隐藏控制槽仍不会显示或响应原版点击；
- 价格格式校验、最低 `1.00`、上架费、物品白名单、余额和库存仍由服务端最终裁决；
- 未新增命令、协议、权限或经济数据写入；
- 既有玩家市场、我的挂单、待领取和返回首页流程不变。

## 本地验证

- PowerShell 7 下执行 `clean test build --no-daemon`；
- Screen JUnit：`78/78`；
- Impeccable layout detector：无发现；
- `git diff --check`：通过；
- JAR：`927,658` 字节，SHA-256：`5219350D7A10E3005DA4AF10DCA1B3EDC21C877050FF05228C67C7F41D6F386F`；
- Test、Gray、Production 和 Minecraft 服务端：本轮未操作。

## 发布前门禁

候选发布前必须重新生成客户端档案并完成：

- 启动器增量更新后真人目视检查上架页四个模块的间距、文字清晰度和窄窗口边界；
- 放入物品、输入价格、确认上架、失败退回和返回流程；
- 双账号市场流程、断线恢复、背包竞争、幂等重试和余额守恒；
- 通过后只进入 Test，保留 `1.0.23 / Screen 0.2.3` 作为回滚目标。
