# 赫朝启动器 0.11.13 发布记录

> 构建日期：`2026-07-27`
>
> 启动器制品源码提交：`dc1d1d527d106fbbc41a58add4bfbd7ae2d12cc9`
>
> Git 标签：`launcher-v0.11.13`
>
> 配套 API：`0.20.0-20260727T011953Z`
>
> 状态：私有 OSS 灰度候选已验证；替换 `0.11.12`

## 变化

- 增加隐私受限的启动器运行遥测，覆盖启动器启动、安装、修复、回滚和游戏启动。
- 事件只使用固定动作、结果、失败分类、启动器版本和档案版本，不上传账号名、
  Minecraft UUID、文件路径、日志正文、会话令牌或私有下载地址。
- 网络不可用时写入有界离线队列；恢复后按幂等批次提交，重复批次不会重复计数。
- 遥测上传失败只保留本地队列并更新内部状态，不阻塞操作，也不会让启动器退出。
- 配套 API 提供 30 天留存、下载量、失败率、启动器版本和档案版本聚合，并接入统一告警。

## 验收证据

- 完整解决方案测试为 `346/346`。
- `tools/Test-WindowsInstaller.ps1` 完成 `0.11.12 -> 0.11.13` 覆盖升级、
  `0.11.13` 干净安装和两轮静默卸载。
- 升级后的 EXE FileVersion 为 `0.11.13.0`，ProductVersion 为
  `0.11.13+dc1d1d527d106fbbc41a58add4bfbd7ae2d12cc9`。
- 注册表 DisplayVersion、开始菜单目标、IconPark `LICENSE` 与 `NOTICE.md` 均通过。
- 安装前后的 `settings.json` 与 DPAPI `session.dat` SHA-256 保持一致。
- 本机原有 `D:\Hechao Launcher` / `0.11.11` 进程未被关闭或覆盖；验证结束后
  注册表和开始菜单恢复到原安装。

## 制品

| 项目 | 值 |
| --- | --- |
| 启动器 EXE | `artifacts/publish/win-x64/Hechao.Launcher.exe` |
| EXE 大小 | `68,755,358` 字节 |
| EXE SHA-256 | `985D4EEA3340D3AC0312DD90954729A348DB04471B947CA1AE52E361ABEAD1DD` |
| 安装包 | `artifacts/installer/Hechao-Launcher-Setup-0.11.13-win-x64.exe` |
| 安装包大小 | `61,868,113` 字节 |
| 安装包 SHA-256 | `E6BF44D9971CEF6D874368E9912158BC60B88A886C652318E94F9D4BE0FFCFE7` |
| Windows 签名 | EXE 与安装包均为 `NotSigned` |

## 私有灰度发布

安装包已写入不可变对象：

```text
releases/launcher/0.11.13/Hechao-Launcher-Setup-0.11.13-win-x64.exe
```

第二次发布确认远端对象匹配并跳过，没有覆盖或重复上传。匿名访问返回 `403`；
24 小时 OSS Bucket 原始节点签名下载返回 `200`，完整下载为 `61,868,113`
字节，SHA-256 与本机制品一致，耗时约 `1.52` 秒。签名链接只保存在当前
管理员账户的 ACL 保护目录，没有写入 Git、文档或公告。

## 回滚

若灰度发现独立阻断，关闭启动器后使用 `0.11.12` 覆盖安装。覆盖不会删除
赫朝会话、设置、客户端、受管 Java、下载缓存或世界存档。服务端遥测接口保持
向后兼容；回滚客户端不会影响 API、档案或游戏服务。

## 边界

本次发布启动器、验证脚本和操作文档，不修改 Minecraft 服务端、Velocity、
客户端档案或数据库结构。没有重启 API、Velocity 或任何 Minecraft 后端，
也没有建立公开下载目录或永久链接。
