# API 0.30.5 发布候选

- 候选版本：`0.30.5`
- 数据库迁移：无，保持 `028`
- 直接回滚目标：`0.30.4-20260814T093000Z`

## 修复范围

- 服务器目录明确区分玩家入口策略与 Java 进程状态；带服控目标的记录可以直接进入
  对应服务器的服控面板；
- 服控页使用 `/admin/control?server=<serverId>` 作为稳定深链接，支持精确选择非默认
  目标；目标不存在或已删除时回退到首个可管理目标并显示告警；
- 立即部署的整合包完成后，详情页核对服控代理、运行目录、活动操作和部署任务身份，
  再显示“启动服务端”交接入口；
- “启动服务端”只打开精确服控目标，不会自动排入启动命令。首次冷启动结果仍以服控
  操作历史和控制台为准。

## 候选验证

- TypeScript 类型检查通过；
- Vitest `11/11`；
- Playwright `30/30`，覆盖非默认目标深链接、无效目标回退、目录直达、部署交接和
  `390px` 移动端无横向溢出；
- API `.NET` `315/315`，完整解决方案 `.NET` `735/735`；
- 发布溯源台账 `20/20` 通过，`git diff --check` 通过；
- 本版本没有改动目录、服控或整合包 API 合同，也没有执行任何 Minecraft 启停命令。

## 发布边界

只发布 API 与其内嵌管理后台。生产切换前必须确认没有正在执行的整合包、服控或数据库
任务，完成数据库 custom-format 备份和当前 API、环境、systemd、Nginx 备份。部署仅允许
重启 `hechao-launcher-api.service`，不得重启 Publisher、Nginx、Velocity、服控代理或
任何 Minecraft 服务端。

安装器就绪检查失败时必须自动恢复 `0.30.4-20260814T093000Z`。部署后验证本机和公网
健康/就绪、管理后台静态资源、旧官网与中转 API、回环监听、服务重启计数及新增
warning/error；任何门禁失败立即回滚。
