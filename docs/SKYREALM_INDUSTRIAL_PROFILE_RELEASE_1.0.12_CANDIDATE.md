# 天域远征工业季客户端档案 1.0.12 候选

- 候选日期：2026-08-17
- 档案 ID：`skyrealm-industrial-neoforge-1.21.1`
- Minecraft：`1.21.1`
- NeoForge：`21.1.228`
- Java：`21`
- 生产状态：已生成并离线验收，尚未上传 OSS 或导入后台
- 清单 SHA-256：
  `25E578B4C1C71996D4787E312D5F62A9BAC879D69ABEAF1B269C449B7577118A`

## 精确差异

候选从生产保存的 `1.0.11` 签名清单重建，不使用包含日志、世界和玩家设置的日常游戏
目录。旧清单先通过正式信任包验签，再从内容寻址对象库恢复文件；唯一缺失的
MixinExtras 对象从已安装档案读取，并按旧清单重新核验大小与 SHA-256。

- `4456` 个共同文件的路径、大小和 SHA-256 全部不变；
- 删除 `mods/HechaoEconomyScreen-NeoForge-1.21.1-0.1.2.jar`；
- 新增 `mods/HechaoEconomyScreen-NeoForge-1.21.1-0.1.3.jar`；
- 同路径内容变化：`0`；
- 逻辑文件：`4457`，逻辑大小：`1,203,308,928` 字节；
- 去重对象：`4252`，对象大小：`1,203,300,418` 字节；
- 新屏幕 JAR：`27,450` 字节，SHA-256
  `68891BD42A91F12036DC18F147DB9576EFAAD5F6B695A8E501CFAAF827F638E3`。

## 验证与发布门禁

- 正式信任包验签：通过；
- 全部对象大小、SHA-256、URL 和无多余对象闭合校验：通过；
- 上传器必须对既有对象执行远端长度和 SHA-256 元数据核验，只上传缺失的新对象；
- 后台只允许导入为不可变 `1.0.12`，先进入 `Test=100%`，不改 Gray 或 Production；
- 服务端 NeoForge 模组必须同时升级到协议 `2`，否则不得开放工业季服务器。

若 Test 出现回归，将 Test 通道回退到 `1.0.11` 清单
`58613946D3CB832C9E06B23CD28669DC562BAE62C40ACB7225F9DE4ED892091C`；不删除或覆盖
`1.0.12` 对象和清单。
