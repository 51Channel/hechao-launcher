# 天域远征工业季客户端档案 1.0.13 Test 发布

- 发布日期：2026-08-18
- 档案 ID：`skyrealm-industrial-neoforge-1.21.1`
- 不可变版本：`1.0.13`
- 清单 SHA-256：
  `5939CEFE4061FEAD5BBEBBF9A3E19F7AAC9656500CADF3BB2932F9E0845103FD`
- Test：`r6 / 1.0.13 / 100%`
- Gray：未分配，`r1`
- Production：未分配，`r1`

## 发布结果

- `1.0.12` 与 `1.0.13` 的 `4,456` 个共同文件路径、大小和 SHA-256 全部一致；
- 删除 Screen `0.1.3`，新增 Screen `0.1.4`，同路径内容变化为 `0`；
- 逻辑文件 `4,457`，逻辑大小 `1,203,312,466` 字节；
- 去重对象 `4,252`，对象大小 `1,203,303,956` 字节；
- OSS 对 `4,251` 个既有对象完成长度与摘要元数据复核，仅上传 `1` 个新对象、
  `30,988` 字节；匿名 OSS 读取返回 `403`，匿名 API 对象读取返回 `401`。

生产 API 已按原始字节保存签名清单，远端文件大小为 `2,025,522` 字节，回读 SHA-256
与本地一致。后台审计分别记录签名版本导入和 Test 通道更新；API 健康、就绪、服务状态
与 `NRestarts=0` 均正常，没有操作任何 Minecraft 或 Velocity 进程。

## 回滚与剩余验收

Test 回滚目标为 `1.0.12` 清单
`25E578B4C1C71996D4787E312D5F62A9BAC879D69ABEAF1B269C449B7577118A`。回滚只移动
Test 指针，不删除 `1.0.13` 清单和对象。

发布时本机工业季游戏进程仍在运行并加载 `1.0.12`，因此没有原地覆盖 JAR。待玩家退出
后，由启动器执行增量更新并重新启动，验收 ESC 菜单中“模组 / 天域远征”双按钮和点击
后的服务端授权屏幕。完成该真人验证前不得推进 Gray 或 Production。
