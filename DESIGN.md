# 赫朝启动器设计系统

## 设计方向

主页是面向玩家高频选服与启动游戏的操作界面。使用清爽白底、赫朝红强调和中等信息密度；整体接近原生 Windows 桌面工具，避免营销式大标题和装饰性卡片堆叠。

## 主页结构

1. 左侧品牌与五个工作区导航，底部显示真实账户与皮肤头像。
2. 中间服务器目录，突出选中项、在线状态、版本和真实人数。
3. 右侧首屏依次展示当前服务器主视区、公告与近期活动、快捷设置。
4. 主页只保留主操作与 Java、内存、当前档案目录三项高频设置；快捷设置使用一条无嵌套卡片的分组控制栏，并给游戏目录保留主要可用宽度。回滚、修复、删除、设置和 Java 模式集中在主按钮右侧的三点菜单中。

## 视觉规则

- 视觉差异度：`6/10`；动效强度：`3/10`；信息密度：`6/10`。
- 使用现有 `HechaoTheme.xaml` 色彩令牌和 `UiFont`。
- 使用现有 IconPark 控件，不新增手绘 SVG。
- 卡片圆角不超过 `8`，不在卡片中嵌套装饰性卡片。
- 正文字号不低于 `12`，关键状态与操作使用 `14-18`，服务器标题使用 `26-30`。
- 路径和版本可使用等宽回退字体；中文正文使用苹方优先字体。
- 默认窗口与最小窗口均不得出现横向滚动、文字重叠或操作按钮裁切。

## 数据规则

- 公告来自服务器目录的 `Announcement`；空值显示明确空状态。
- 近期活动来自活动月历 ViewModel 的真实排期；无排期时显示空状态并提供活动页入口。
- 服务器图片使用仓库内现有真实品牌资产，未提供逐服图片时不伪造差异化图片。
- 不增加虚假的延迟、标签、收藏、玩家人数或管理员入口。

## 交互规则

- 服务器选择使用现有 `SelectedServer` 双向绑定，并在长任务期间禁用。
- 主按钮继续使用 `PrimaryActionCommand`，其安装、更新、登录、绑定和启动状态由 ViewModel 决定。
- Java 和内存继续写入现有设置；主页目录只显示并打开所选档案的 `instances\<profile-id>\.minecraft`，不得在这里显示或修改全局 `GameData` 根目录。全局数据根目录只在设置页修改。
- 自定义 Java、修复、回滚和删除客户端保持可达。
- 通知、活动、设置等次级区域使用现有命令进入对应工作区或抽屉。
