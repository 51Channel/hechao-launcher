# 赫朝启动器设计系统

## 当前方向

用户已确认采用 A 方案：服务器目录 + 右侧详情工作台。它借鉴 Aaalice Workflow Hub 的目录与 Inspector 逻辑，但保留赫朝启动器现有业务语义。本阶段只调整视觉观感、操作手感和入口层级，不扩展功能，也不修改真实服务器状态。

正式 WPF 已同时提供日间与黑夜模式。两种主题共享完全相同的布局、控件尺寸、信息顺序和操作路径，只替换语义颜色与表面层级。

## 页面结构

1. 顶部栏固定承载品牌、服务器/活动/下载中心三个主工作区、通知、主题、设置、账户与窗口控制。
2. 主工作区左侧为可扫描的服务器目录。标题、同步状态和刷新操作位于目录顶部，服务器项展示真实名称、说明、版本、加载器、状态和人数。
3. 选中服务器用赫朝红细边界标记。服务器项本身是进入详情的操作对象，不增加搜索、收藏或延迟等未确认功能。
4. 右侧 Inspector 固定展示当前服务器详情、客户端状态、运行设置和唯一主操作。详情内容内部滚动，底部主按钮固定，不随内容长度漂移。
5. 启动器默认以 `1200 x 720` 的紧凑双列工作台窗口打开，并允许玩家继续调整或最大化；`1060 x 640` 保留为最小窗口边界。默认窗口下服务器卡片稳定排列两列，缩至最小窗口时自然回落单列；账户入口、主题按钮、设置和三个窗口控制键始终保留。
6. 永久服卡片保留城堡封面；活动服复用目录已有的 `IconGlyph` 显示活动识别面，不扩展图片协议。顶栏账户只显示简短身份，完整权限组仍在账户页呈现。

### 下载中心

- 左侧使用紧凑下载历史行，任务名称、传输阶段、进度、版本、时间和结果各自占据稳定区域；长名称不再与百分比争抢同一位置。
- 打开游戏目录与清空历史位于页面标题区，清空历史降为低干扰图标入口。
- 右侧传输 Inspector 固定展示当前任务或明确空状态；当前目录和取消任务集中于 Inspector，底部操作区不随详情长度漂移。

### 活动

- 左侧保留完整六周月历，月份切换与“今天”位于月历顶栏；日期格只承载日期、状态点和紧凑活动名称。
- 右侧活动详情 Inspector 同时容纳选中日期活动和待排期活动，准备客户端继续使用既有命令与权限判断。
- 页面本身不再整页滚动；只有右侧详情区在最小窗口下局部滚动，避免月历、活动列表形成嵌套滚动。
- 月历外框与主要区块使用结构分隔线，重复日期格只保留低对比横向发丝线，不绘制竖向方格线；右侧重复条目使用与正文对齐的内缩发丝线。选中日期继续由赫朝红细框识别。

### 赫朝账户

- 左侧保留身份摘要、当前权限和会话安全；右侧根据登录状态显示登录/注册工作区或 Minecraft 正版身份与 LuckPerms 权限详情。
- 退出全部设备、解除绑定等高影响操作使用明确的危险层级，普通退出与查看信息保持次级层级。
- 登录、注册、绑定、解绑确认和管理员入口继续使用原有 ViewModel 与代码路径，不改变账户协议。

### 设置

- 左侧固定为“游戏与运行、客户端与下载、启动器行为、故障诊断”四类目录，右侧一次只展示当前分类。
- 每个分类拥有自己的内容滚动区；设置页根部不再使用整页滚动，分类切换不会改变左右分栏几何。
- 普通开关、立即执行、清空/恢复默认和诊断上传按影响程度分层；全部现有绑定与即时保存行为保持不变。
- 诊断包仍先在本机生成并脱敏，上传继续使用独立入口与原有确认流程，不会自动上传。

## 主题与颜色

### 黑夜模式

- 页面底色：`#111315`
- 顶部栏：`#141618`
- 主要表面：`#1a1d20`
- 抬升表面：`#21262b`
- 主文字：`#d6dadc`
- 次要文字：`#adb3b7`
- 主操作：`#a73d34`
- 主操作悬停：`#b64a3f`
- 结构分隔线：`#32383e`
- 重复发丝线：`#252a2e`

### 日间模式

- 页面底色：`#f3f7fa`
- 顶部栏：`#e7eef3`
- 主要表面：`#ffffff`
- 抬升表面：`#fbfcfd`
- 主文字：`#17242c`
- 主操作：`#c74636`
- 主操作悬停：`#b7372a`
- 结构分隔线：`#cbd6dd`
- 重复发丝线：`#e3e9ed`

日间模式使用冷白与蓝灰层级，不使用米色大底。赫朝红只表示当前选择、焦点和主操作；状态色只用于成功、提醒和错误语义。

## 开关灯按钮

- 位于通知与设置之间，复用顶部栏 `38 x 38` 图标按钮规格。
- 黑夜模式显示月亮，日间模式显示太阳；按钮位置和占位始终不变。
- 可访问名称固定为“黑夜模式”，WPF `ToggleButton` 的选中状态表示黑夜模式已开启。
- 悬停标题描述下一步动作：黑夜时为“切换到日间模式”，日间时为“切换到黑夜模式”。
- 正式 WPF 将选择保存为 `LauncherSettings.UseDarkMode`，启动时先应用主题再创建窗口，避免主题闪烁。
- 首次启动默认黑夜模式；已有设置会继续使用用户上次的选择。

## 视觉规则

- 信息密度为中等偏紧凑，服务玩家反复浏览和启动，不做营销式首屏。
- 卡片圆角不超过 `8px`，不在卡片中嵌套装饰性卡片。
- 字号、间距、边界、阴影和颜色使用语义令牌；切换主题不得改变几何尺寸。
- 顶栏使用应用图标加“赫朝启动器”文字作为稳定品牌锚点；三个工作区入口采用中性分段导航，不用大面积红底抢占主任务注意力。
- 黑夜模式的顶栏、目录、Inspector 顶栏和正文表面保持可辨认的明度差；卡片主要依靠表面与细边界建立层级，不批量叠加阴影。
- 线条分为控件边框、结构分隔线与重复发丝线三层；密集内容不得用同一强度的完整方格描边。
- 页面标题与节标题使用中等字重和紧凑尺度，正文优先使用中文系统字体回退；路径、版本等数据仍可使用等宽字体。
- 主按钮在两种主题下均使用足够深的红色承载白字，不使用亮红底白字的低对比组合。
- 赫朝红只用于主操作、焦点、危险操作和细选中标记；普通悬停、导航选中与设置分类选中使用中性抬升表面。
- 图标按钮必须使用统一图标体系，并具有键盘焦点环、悬停和按下反馈；按钮、服务器项和主题开关按下时提供 `1px` 位移，陌生图标提供标题说明。
- 默认窗口与最小窗口均不得出现横向滚动、文字重叠或主操作裁切。
- 默认 `1200 x 720` 下使用 `420px` Inspector，服务器目录必须容纳两张 `342 x 204px` 卡片及 `14px` 间距；最小窗口允许 WrapPanel 自动回落为单列，不继续压缩卡片内容。

## 数据与功能边界

- 保留服务器、下载中心、活动、账户、设置五个入口。
- 使用当前已有的服务器、版本、状态、权限、人数、客户端和运行设置语义。
- 主按钮继续映射现有 `PrimaryActionCommand`，其安装、更新、登录、绑定和启动状态仍由 ViewModel 决定。
- 不新增搜索、收藏、延迟、商店、社交、管理员入口或虚构服务器数据。
- 通知、设置、活动等入口保持可达；本轮不重新设计其内部业务流程。

## 正式 WPF 实现

- 主题偏好写入现有 `%LocalAppData%\Hechao\Launcher\settings.json` 的 `UseDarkMode` 字段，不复用游戏数据布局版本号；缺失字段按黑夜模式处理，兼容旧设置文件。
- `LauncherThemeService` 原地同步 `DarkPalette.xaml` 与 `LightPalette.xaml`，切换前先校验全部共享语义 Brush 可更新，再用 `SetCurrentValue` 保留其动态颜色表达式；任何色刷被冻结时会在修改 Palette 和偏好前整体失败，避免留下混合主题。业务 ViewModel 只保存布尔偏好，不直接操作颜色值。
- `MainWindow.xaml` 与 `HechaoTheme.xaml` 的主题 Brush 使用点统一采用 `DynamicResource`，因此运行时切换会刷新已加载的样式与模板，不重建控件或改变布局几何。
- `--ui-preview=dark|light`、`--ui-preview-page=servers|downloads|activities|account|settings`、`--ui-preview-switch-theme=dark|light` 与截图参数仅在 Debug 构建可用，并使用系统临时目录、内存设置和 Null/Stub 服务，不读取真实账号、不联网、不启动游戏。

## 当前验证

- 黑夜模式已改为“柔和石墨”：主文字对画布/表面为 `13.23:1` / `12.03:1`，次文字对表面/抬升表面为 `7.99:1` / `7.20:1`，最弱文字在最亮按下表面仍为 `4.56:1`；主按钮悬停态白字为 `5.20:1`。
- 真实 WPF 验证覆盖五个工作区、服务器页 `1060 x 640` / `1200 x 720` / `1700 x 900` 及运行时双向主题切换。白天结果与改色前服务器页哈希一致，白转黑与黑夜冷启动哈希一致。
- 服务器、下载、活动、账户与设置均已落入同一套真实 WPF 工作台结构；日间/黑夜、默认 `1200 x 720` 与最小 `1060 x 640` 均已完成视觉验证。
- 顶栏品牌与显式图标、中性导航选中态、独立 Inspector 材质层、字体层级、主题通知图标和按压反馈均已进入自动合同测试。
- 主题保存回读、旧设置默认值、切换命令、可访问名称、应用启动顺序、双主题资源和五个工作区结构继续由既有自动测试覆盖。
- 当前启动器测试为 Debug `286/286`、Release `271/271`；Release 全解决方案为 `863` 通过、`1` 个 PostgreSQL 环境测试按设计跳过、`0` 失败。
- `1200 x 720` 默认窗口下服务器目录已稳定显示两列，`1060 x 640` 最小窗口下自动回落单列；顶部入口顺序为“服务器 → 活动 → 下载中心”。两种尺寸的顶栏入口、Inspector、月历和主要操作均保持可见，无横向溢出、重叠或裁切。
- 活动页两级线条已通过深浅主题 `1200 x 720`、`1060 x 640` 及黑夜 `1700 x 900` 复核；大窗口、默认窗口与最小窗口均保持日期列可扫描，右侧详情层级清楚。
- 服务器页与活动页的黑夜→白天、白天→黑夜运行时切换结果均与对应冷启动图逐字节一致。
- 本次 UI 变更不包含安装包构建、更新通道发布或正式版替换。
